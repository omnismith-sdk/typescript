/* tslint:disable */
/* eslint-disable */
/**
 * Omnismith API
 * API Documentation for Omnismith
 *
 * The version of the OpenAPI document: 1.0.13
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { EntityAttributesInputValue } from './EntityAttributesInputValue';
import {
    EntityAttributesInputValueFromJSON,
    EntityAttributesInputValueFromJSONTyped,
    EntityAttributesInputValueToJSON,
    EntityAttributesInputValueToJSONTyped,
} from './EntityAttributesInputValue';

/**
 * The records to run the action on and the values for its fields, shared by every record.
 * @export
 * @interface BatchExecuteEntityActionRequest
 */
export interface BatchExecuteEntityActionRequest {
    /**
     * Distinct entity identifiers, at most 100 per call. Every record must belong to a template that defines the action.
     * @type {Array<string>}
     * @memberof BatchExecuteEntityActionRequest
     */
    entity_ids: Array<string>;
    /**
     * Map of attribute slug or UUID → value. Keys may be mixed freely in one request; each attribute may appear once.
     * 
     * ### Values
     * - **Plain scalar** — `"text"`, `42`, `129.99`, `true`, or `null`. Numbers and booleans are serialized for you (`42` → `"42"`, `true` → `"true"`); floats keep exactly the digits you sent.
     * - **`null`** — clears the attribute.
     * - **Backfill object** — `{ "value": <scalar|null>, "updated_at": "2026-09-12T12:23:52Z" }` records the value as observed at that moment (history import). `updated_at` must be RFC 3339 with an explicit offset (`Z` or `±HH:MM`); it defaults to now when omitted.
     * - **Operation object** — `{ "op": "increment", "value": 1 }` adds the number to the stored value instead of overwriting it: you never need to read the current value, and concurrent increments are serialized so none is lost. Allowed on **Number** attributes and **Metrics** only (a metric increment appends `latest + value` as a new observation); a never-set attribute counts as `0`; a negative `value` subtracts. `op` cannot be combined with `updated_at`. Not accepted on create. The change log records the resolved value, never the operand.
     * 
     * ### Value format by attribute type
     * - **Text / Markdown**: UTF-8 string
     * - **Number**: number or numeric string (`129.99`, `"42"`)
     * - **Boolean**: `true` / `false` (or `"true"` / `"false"`, `"1"` / `"0"`)
     * - **Date**: `YYYY-MM-DD`; **Datetime**: `YYYY-MM-DD HH:MM:SS` or ISO 8601 `YYYY-MM-DDTHH:MM:SSZ`
     * - **File / Image**: UUID of a previously uploaded file asset
     * - **List**: UUID of one of the attribute's list items
     * - **Reference**: UUID of the referenced entity
     * - **Metric**: numeric observation; appended to the time series (never overwritten)
     * 
     * ### Example
     * ```json
     * {
     *   "hostname": "edge-fra-01",
     *   "cpu_cores": 8,
     *   "notes": null,
     *   "operational_status": { "value": "Active", "updated_at": "2026-09-12T12:23:52Z" },
     *   "restart_count": { "op": "increment", "value": 1 }
     * }
     * ```
     * @type {{ [key: string]: EntityAttributesInputValue; }}
     * @memberof BatchExecuteEntityActionRequest
     */
    values?: { [key: string]: EntityAttributesInputValue; };
    /**
     * When false (the default) every record is attempted and its outcome reported. When true the batch runs in one transaction and the first record that does not execute rolls all of it back.
     * @type {boolean}
     * @memberof BatchExecuteEntityActionRequest
     */
    atomic?: boolean;
}

/**
 * Check if a given object implements the BatchExecuteEntityActionRequest interface.
 */
export function instanceOfBatchExecuteEntityActionRequest(value: object): value is BatchExecuteEntityActionRequest {
    if (!('entity_ids' in value) || value['entity_ids'] === undefined) return false;
    return true;
}

export function BatchExecuteEntityActionRequestFromJSON(json: any): BatchExecuteEntityActionRequest {
    return BatchExecuteEntityActionRequestFromJSONTyped(json, false);
}

export function BatchExecuteEntityActionRequestFromJSONTyped(json: any, ignoreDiscriminator: boolean): BatchExecuteEntityActionRequest {
    if (json == null) {
        return json;
    }
    return {
        
        'entity_ids': json['entity_ids'],
        'values': json['values'] == null ? undefined : (mapValues(json['values'], EntityAttributesInputValueFromJSON)),
        'atomic': json['atomic'] == null ? undefined : json['atomic'],
    };
}

export function BatchExecuteEntityActionRequestToJSON(json: any): BatchExecuteEntityActionRequest {
    return BatchExecuteEntityActionRequestToJSONTyped(json, false);
}

export function BatchExecuteEntityActionRequestToJSONTyped(value?: BatchExecuteEntityActionRequest | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'entity_ids': value['entity_ids'],
        'values': value['values'] == null ? undefined : (mapValues(value['values'], EntityAttributesInputValueToJSON)),
        'atomic': value['atomic'],
    };
}

