/* tslint:disable */
/* eslint-disable */
/**
 * Omnismith API
 * API Documentation for Omnismith
 *
 * The version of the OpenAPI document: 1.0.15
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { EntityAttributeValue } from './EntityAttributeValue';
import {
    instanceOfEntityAttributeValue,
    EntityAttributeValueFromJSON,
    EntityAttributeValueFromJSONTyped,
    EntityAttributeValueToJSON,
} from './EntityAttributeValue';

/**
 * @type EntityResponseAttributeValues
 * Attribute values of the entity. The shape depends on the `verbose` flag of the request:
 * - default (compact): an object mapping attribute slug to the display value, e.g. `{"price": "129.99", "status": "Open", "owner": "Platform team"}`. List, reference and file attributes show their resolved label (list option label, referenced entity display value, original filename); the stored ids behind those labels are in `list_item_ids`, `reference_entity_ids` and `file_ids`. Attributes without a slug are keyed by their UUID. Attributes whose value is empty are omitted.
 * - `verbose=true`: an array of `EntityAttributeValue` items, one per attribute (empty values included), each carrying the attribute `id`, `slug`, raw `value`, resolved `custom_value` and `reference_entity_id`.
 * @export
 */
export type EntityResponseAttributeValues = Array<EntityAttributeValue> | { [key: string]: string; };

export function EntityResponseAttributeValuesFromJSON(json: any): EntityResponseAttributeValues {
    return EntityResponseAttributeValuesFromJSONTyped(json, false);
}

export function EntityResponseAttributeValuesFromJSONTyped(json: any, ignoreDiscriminator: boolean): EntityResponseAttributeValues {
    if (json == null) {
        return json;
    }
    if (Array.isArray(json)) {
        if (json.every(item => typeof item === 'object')) {
            if (json.every(item => instanceOfEntityAttributeValue(item))) {
                return json.map(value => EntityAttributeValueFromJSONTyped(value, true));
            }
        }
        return json;
    }
    return {} as any;
}

export function EntityResponseAttributeValuesToJSON(json: any): any {
    return EntityResponseAttributeValuesToJSONTyped(json, false);
}

export function EntityResponseAttributeValuesToJSONTyped(value?: EntityResponseAttributeValues | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }
    if (Array.isArray(value)) {
        if (value.every(item => typeof item === 'object')) {
            if (value.every(item => instanceOfEntityAttributeValue(item))) {
                return value.map(value => EntityAttributeValueToJSON(value as EntityAttributeValue));
            }
        }
        return value;
    }
    return {};
}

