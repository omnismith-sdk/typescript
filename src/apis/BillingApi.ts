/* tslint:disable */
/* eslint-disable */
/**
 * Omnismith API
 * API Documentation for Omnismith
 *
 * The version of the OpenAPI document: 1.0.14
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CheckoutResponse,
  CreateCheckoutRequest,
  GetAllTiers200Response,
  LogAiUsage200Response,
  LogAiUsageRequest,
  PortalUrlResponse,
  TierResponse,
  UsageInsightsResponse,
  ValidationErrorResponse,
} from '../models/index';
import {
    CheckoutResponseFromJSON,
    CheckoutResponseToJSON,
    CreateCheckoutRequestFromJSON,
    CreateCheckoutRequestToJSON,
    GetAllTiers200ResponseFromJSON,
    GetAllTiers200ResponseToJSON,
    LogAiUsage200ResponseFromJSON,
    LogAiUsage200ResponseToJSON,
    LogAiUsageRequestFromJSON,
    LogAiUsageRequestToJSON,
    PortalUrlResponseFromJSON,
    PortalUrlResponseToJSON,
    TierResponseFromJSON,
    TierResponseToJSON,
    UsageInsightsResponseFromJSON,
    UsageInsightsResponseToJSON,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/index';

export interface CreateCheckoutOperationRequest {
    createCheckoutRequest: CreateCheckoutRequest;
}

export interface LogAiUsageOperationRequest {
    logAiUsageRequest: LogAiUsageRequest;
}

/**
 * BillingApi - interface
 * 
 * @export
 * @interface BillingApiInterface
 */
export interface BillingApiInterface {
    /**
     * Creates request options for createCheckout without sending the request
     * @param {CreateCheckoutRequest} createCheckoutRequest 
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    createCheckoutRequestOpts(requestParameters: CreateCheckoutOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Creates a LemonSqueezy checkout session to subscribe to a paid tier. Returns a URL to redirect the user to complete payment.
     * @summary Create a checkout session for subscription
     * @param {CreateCheckoutRequest} createCheckoutRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    createCheckoutRaw(requestParameters: CreateCheckoutOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CheckoutResponse>>;

    /**
     * Creates a LemonSqueezy checkout session to subscribe to a paid tier. Returns a URL to redirect the user to complete payment.
     * Create a checkout session for subscription
     */
    createCheckout(requestParameters: CreateCheckoutOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CheckoutResponse>;

    /**
     * Creates request options for getAllTiers without sending the request
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    getAllTiersRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Returns the full catalog of subscription tiers (Free, Starter, Pro, etc.) with their resource limits including max attributes, templates, entities, dashboards, automations, notification channels, metric ingestions, dimension updates, disk storage, and AI credits. Does not require authentication.
     * @summary List all available tiers
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    getAllTiersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetAllTiers200Response>>;

    /**
     * Returns the full catalog of subscription tiers (Free, Starter, Pro, etc.) with their resource limits including max attributes, templates, entities, dashboards, automations, notification channels, metric ingestions, dimension updates, disk storage, and AI credits. Does not require authentication.
     * List all available tiers
     */
    getAllTiers(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetAllTiers200Response>;

    /**
     * Creates request options for getPortalUrl without sending the request
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    getPortalUrlRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Returns a LemonSqueezy customer portal URL where users can manage their subscription (cancel, update payment method, view invoices).
     * @summary Get customer portal URL
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    getPortalUrlRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PortalUrlResponse>>;

    /**
     * Returns a LemonSqueezy customer portal URL where users can manage their subscription (cancel, update payment method, view invoices).
     * Get customer portal URL
     */
    getPortalUrl(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PortalUrlResponse>;

    /**
     * Creates request options for getUsageInsights without sending the request
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    getUsageInsightsRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Returns current resource consumption vs. tier limits for the authenticated user. Includes usage counts (attributes, templates, entities, dashboards, automations, channels, monthly metric ingestions, monthly dimension updates, disk usage, AI credits), corresponding tier limits, and percentage utilization for each resource category. Use this to check quota availability before performing operations.
     * @summary Get current tier usage insights
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    getUsageInsightsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UsageInsightsResponse>>;

    /**
     * Returns current resource consumption vs. tier limits for the authenticated user. Includes usage counts (attributes, templates, entities, dashboards, automations, channels, monthly metric ingestions, monthly dimension updates, disk usage, AI credits), corresponding tier limits, and percentage utilization for each resource category. Use this to check quota availability before performing operations.
     * Get current tier usage insights
     */
    getUsageInsights(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UsageInsightsResponse>;

    /**
     * Creates request options for getUserTier without sending the request
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    getUserTierRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Returns the authenticated user\'s active subscription tier, including tier name, level, and all associated resource limits (max entities, attributes, templates, metric ingestions, AI credits, etc.).
     * @summary Get current user tier
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    getUserTierRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TierResponse>>;

    /**
     * Returns the authenticated user\'s active subscription tier, including tier name, level, and all associated resource limits (max entities, attributes, templates, metric ingestions, AI credits, etc.).
     * Get current user tier
     */
    getUserTier(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TierResponse>;

    /**
     * Creates request options for logAiUsage without sending the request
     * @param {LogAiUsageRequest} logAiUsageRequest 
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    logAiUsageRequestOpts(requestParameters: LogAiUsageOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Records AI credit consumption for the authenticated user. Accepts model name, input/output token counts, and computed credits to deduct. Used internally by the AI service to track per-request LLM costs against the user\'s monthly AI credit quota.
     * @summary Log AI usage credits
     * @param {LogAiUsageRequest} logAiUsageRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof BillingApiInterface
     */
    logAiUsageRaw(requestParameters: LogAiUsageOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LogAiUsage200Response>>;

    /**
     * Records AI credit consumption for the authenticated user. Accepts model name, input/output token counts, and computed credits to deduct. Used internally by the AI service to track per-request LLM costs against the user\'s monthly AI credit quota.
     * Log AI usage credits
     */
    logAiUsage(requestParameters: LogAiUsageOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LogAiUsage200Response>;

}

/**
 * 
 */
export class BillingApi extends runtime.BaseAPI implements BillingApiInterface {

    /**
     * Creates request options for createCheckout without sending the request
     */
    async createCheckoutRequestOpts(requestParameters: CreateCheckoutOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['createCheckoutRequest'] == null) {
            throw new runtime.RequiredError(
                'createCheckoutRequest',
                'Required parameter "createCheckoutRequest" was null or undefined when calling createCheckout().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/billing/checkout`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateCheckoutRequestToJSON(requestParameters['createCheckoutRequest']),
        };
    }

    /**
     * Creates a LemonSqueezy checkout session to subscribe to a paid tier. Returns a URL to redirect the user to complete payment.
     * Create a checkout session for subscription
     */
    async createCheckoutRaw(requestParameters: CreateCheckoutOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CheckoutResponse>> {
        const requestOptions = await this.createCheckoutRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CheckoutResponseFromJSON(jsonValue));
    }

    /**
     * Creates a LemonSqueezy checkout session to subscribe to a paid tier. Returns a URL to redirect the user to complete payment.
     * Create a checkout session for subscription
     */
    async createCheckout(requestParameters: CreateCheckoutOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CheckoutResponse> {
        const response = await this.createCheckoutRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getAllTiers without sending the request
     */
    async getAllTiersRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/billing/tiers`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the full catalog of subscription tiers (Free, Starter, Pro, etc.) with their resource limits including max attributes, templates, entities, dashboards, automations, notification channels, metric ingestions, dimension updates, disk storage, and AI credits. Does not require authentication.
     * List all available tiers
     */
    async getAllTiersRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetAllTiers200Response>> {
        const requestOptions = await this.getAllTiersRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetAllTiers200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns the full catalog of subscription tiers (Free, Starter, Pro, etc.) with their resource limits including max attributes, templates, entities, dashboards, automations, notification channels, metric ingestions, dimension updates, disk storage, and AI credits. Does not require authentication.
     * List all available tiers
     */
    async getAllTiers(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetAllTiers200Response> {
        const response = await this.getAllTiersRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getPortalUrl without sending the request
     */
    async getPortalUrlRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/billing/portal`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns a LemonSqueezy customer portal URL where users can manage their subscription (cancel, update payment method, view invoices).
     * Get customer portal URL
     */
    async getPortalUrlRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PortalUrlResponse>> {
        const requestOptions = await this.getPortalUrlRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PortalUrlResponseFromJSON(jsonValue));
    }

    /**
     * Returns a LemonSqueezy customer portal URL where users can manage their subscription (cancel, update payment method, view invoices).
     * Get customer portal URL
     */
    async getPortalUrl(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PortalUrlResponse> {
        const response = await this.getPortalUrlRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getUsageInsights without sending the request
     */
    async getUsageInsightsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/billing/usage/insights`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns current resource consumption vs. tier limits for the authenticated user. Includes usage counts (attributes, templates, entities, dashboards, automations, channels, monthly metric ingestions, monthly dimension updates, disk usage, AI credits), corresponding tier limits, and percentage utilization for each resource category. Use this to check quota availability before performing operations.
     * Get current tier usage insights
     */
    async getUsageInsightsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UsageInsightsResponse>> {
        const requestOptions = await this.getUsageInsightsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UsageInsightsResponseFromJSON(jsonValue));
    }

    /**
     * Returns current resource consumption vs. tier limits for the authenticated user. Includes usage counts (attributes, templates, entities, dashboards, automations, channels, monthly metric ingestions, monthly dimension updates, disk usage, AI credits), corresponding tier limits, and percentage utilization for each resource category. Use this to check quota availability before performing operations.
     * Get current tier usage insights
     */
    async getUsageInsights(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UsageInsightsResponse> {
        const response = await this.getUsageInsightsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getUserTier without sending the request
     */
    async getUserTierRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/billing/tier`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the authenticated user\'s active subscription tier, including tier name, level, and all associated resource limits (max entities, attributes, templates, metric ingestions, AI credits, etc.).
     * Get current user tier
     */
    async getUserTierRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TierResponse>> {
        const requestOptions = await this.getUserTierRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TierResponseFromJSON(jsonValue));
    }

    /**
     * Returns the authenticated user\'s active subscription tier, including tier name, level, and all associated resource limits (max entities, attributes, templates, metric ingestions, AI credits, etc.).
     * Get current user tier
     */
    async getUserTier(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TierResponse> {
        const response = await this.getUserTierRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for logAiUsage without sending the request
     */
    async logAiUsageRequestOpts(requestParameters: LogAiUsageOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['logAiUsageRequest'] == null) {
            throw new runtime.RequiredError(
                'logAiUsageRequest',
                'Required parameter "logAiUsageRequest" was null or undefined when calling logAiUsage().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/billing/log-usage`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LogAiUsageRequestToJSON(requestParameters['logAiUsageRequest']),
        };
    }

    /**
     * Records AI credit consumption for the authenticated user. Accepts model name, input/output token counts, and computed credits to deduct. Used internally by the AI service to track per-request LLM costs against the user\'s monthly AI credit quota.
     * Log AI usage credits
     */
    async logAiUsageRaw(requestParameters: LogAiUsageOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<LogAiUsage200Response>> {
        const requestOptions = await this.logAiUsageRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => LogAiUsage200ResponseFromJSON(jsonValue));
    }

    /**
     * Records AI credit consumption for the authenticated user. Accepts model name, input/output token counts, and computed credits to deduct. Used internally by the AI service to track per-request LLM costs against the user\'s monthly AI credit quota.
     * Log AI usage credits
     */
    async logAiUsage(requestParameters: LogAiUsageOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<LogAiUsage200Response> {
        const response = await this.logAiUsageRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
