/* tslint:disable */
/* eslint-disable */
/**
 * Omnismith API
 * API Documentation for Omnismith
 *
 * The version of the OpenAPI document: 1.0.13
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ErrorResponse,
  GetMyPermissions200Response,
  GoogleLogin200Response,
  GoogleLoginRequest,
  ListSessions200Response,
  LoginRequest,
  RefreshToken200Response,
  RefreshToken401Response,
  RefreshTokenRequest,
  ValidationErrorResponse,
} from '../models/index';
import {
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    GetMyPermissions200ResponseFromJSON,
    GetMyPermissions200ResponseToJSON,
    GoogleLogin200ResponseFromJSON,
    GoogleLogin200ResponseToJSON,
    GoogleLoginRequestFromJSON,
    GoogleLoginRequestToJSON,
    ListSessions200ResponseFromJSON,
    ListSessions200ResponseToJSON,
    LoginRequestFromJSON,
    LoginRequestToJSON,
    RefreshToken200ResponseFromJSON,
    RefreshToken200ResponseToJSON,
    RefreshToken401ResponseFromJSON,
    RefreshToken401ResponseToJSON,
    RefreshTokenRequestFromJSON,
    RefreshTokenRequestToJSON,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/index';

export interface GetMyPermissionsRequest {
    xOmnismithProjectId?: string;
}

export interface GoogleLoginOperationRequest {
    googleLoginRequest: GoogleLoginRequest;
}

export interface GoogleLoginRedirectRequest {
    credential: string;
    gCsrfToken?: string | null;
}

export interface LoginOperationRequest {
    loginRequest: LoginRequest;
}

export interface RefreshTokenOperationRequest {
    refreshTokenRequest: RefreshTokenRequest;
}

export interface RevokeSessionRequest {
    id: string;
}

/**
 * AuthApi - interface
 * 
 * @export
 * @interface AuthApiInterface
 */
export interface AuthApiInterface {
    /**
     * Creates request options for getMyPermissions without sending the request
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    getMyPermissionsRequestOpts(requestParameters: GetMyPermissionsRequest): Promise<runtime.RequestOpts>;

    /**
     * Returns the complete list of permission keys granted to the authenticated user or agent under their active project role. Call this endpoint before planning or executing multi-step schema modifications, role administration, or entity mutations to verify current operational capabilities. Returns `[\"*\"]` for project owners who possess root administrative privileges, or an array of granular permission keys (e.g. `entity.view`, `entity.create`, `template.create`, `billing.view_usage`) for assigned roles. Returns an empty array if no role is currently assigned.
     * @summary Discover authenticated caller permissions and capabilities
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    getMyPermissionsRaw(requestParameters: GetMyPermissionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetMyPermissions200Response>>;

    /**
     * Returns the complete list of permission keys granted to the authenticated user or agent under their active project role. Call this endpoint before planning or executing multi-step schema modifications, role administration, or entity mutations to verify current operational capabilities. Returns `[\"*\"]` for project owners who possess root administrative privileges, or an array of granular permission keys (e.g. `entity.view`, `entity.create`, `template.create`, `billing.view_usage`) for assigned roles. Returns an empty array if no role is currently assigned.
     * Discover authenticated caller permissions and capabilities
     */
    getMyPermissions(requestParameters: GetMyPermissionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetMyPermissions200Response>;

    /**
     * Creates request options for googleLogin without sending the request
     * @param {GoogleLoginRequest} googleLoginRequest 
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    googleLoginRequestOpts(requestParameters: GoogleLoginOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Authenticates a user using a Google Identity Services (GIS) ID token. Verifies the cryptographic token signature against Google public keys. If no account exists for the verified email address, a new user account is automatically provisioned and verified. Returns a JWT access token and refresh token for the active session.
     * @summary Authenticate or register with Google Sign-In
     * @param {GoogleLoginRequest} googleLoginRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    googleLoginRaw(requestParameters: GoogleLoginOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GoogleLogin200Response>>;

    /**
     * Authenticates a user using a Google Identity Services (GIS) ID token. Verifies the cryptographic token signature against Google public keys. If no account exists for the verified email address, a new user account is automatically provisioned and verified. Returns a JWT access token and refresh token for the active session.
     * Authenticate or register with Google Sign-In
     */
    googleLogin(requestParameters: GoogleLoginOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GoogleLogin200Response>;

    /**
     * Creates request options for googleLoginRedirect without sending the request
     * @param {string} credential Google ID token credential issued by Google Identity Services
     * @param {string} [gCsrfToken] CSRF token provided by Google Identity Services
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    googleLoginRedirectRequestOpts(requestParameters: GoogleLoginRedirectRequest): Promise<runtime.RequestOpts>;

    /**
     * Handles Google Identity Services form-urlencoded POST redirection (`credential` and `g_csrf_token`). Authenticates or provisions the user account, initiates an active session, and redirects the browser (HTTP 302) to the frontend application callback URL with JWT access and refresh tokens embedded in the URL fragment.
     * @summary Google OAuth callback redirect handler
     * @param {string} credential Google ID token credential issued by Google Identity Services
     * @param {string} [gCsrfToken] CSRF token provided by Google Identity Services
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    googleLoginRedirectRaw(requestParameters: GoogleLoginRedirectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Handles Google Identity Services form-urlencoded POST redirection (`credential` and `g_csrf_token`). Authenticates or provisions the user account, initiates an active session, and redirects the browser (HTTP 302) to the frontend application callback URL with JWT access and refresh tokens embedded in the URL fragment.
     * Google OAuth callback redirect handler
     */
    googleLoginRedirect(requestParameters: GoogleLoginRedirectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * Creates request options for listSessions without sending the request
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    listSessionsRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Retrieves all login sessions recorded for the authenticated user across devices and browsers. Each session record includes client metadata (IP address, User-Agent), issuance timestamp, expiration date, current status (`active`, `expired`, `revoked`), and revocation details if applicable.
     * @summary List active and historical user sessions
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    listSessionsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListSessions200Response>>;

    /**
     * Retrieves all login sessions recorded for the authenticated user across devices and browsers. Each session record includes client metadata (IP address, User-Agent), issuance timestamp, expiration date, current status (`active`, `expired`, `revoked`), and revocation details if applicable.
     * List active and historical user sessions
     */
    listSessions(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListSessions200Response>;

    /**
     * Creates request options for login without sending the request
     * @param {LoginRequest} loginRequest 
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    loginRequestOpts(requestParameters: LoginOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Authenticates an existing user account using email and password. Upon successful validation, generates an active login session and returns a short-lived JWT access token and a refresh token for rotating credentials.
     * @summary Authenticate user with email and password
     * @param {LoginRequest} loginRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    loginRaw(requestParameters: LoginOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GoogleLogin200Response>>;

    /**
     * Authenticates an existing user account using email and password. Upon successful validation, generates an active login session and returns a short-lived JWT access token and a refresh token for rotating credentials.
     * Authenticate user with email and password
     */
    login(requestParameters: LoginOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GoogleLogin200Response>;

    /**
     * Creates request options for refreshToken without sending the request
     * @param {RefreshTokenRequest} refreshTokenRequest 
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    refreshTokenRequestOpts(requestParameters: RefreshTokenOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Exchanges a valid refresh token for a newly issued JWT access token and a rotated refresh token. Implements strict single-use refresh token rotation: the supplied refresh token is permanently invalidated upon successful exchange. If an expired, already-rotated, or revoked token is presented, the request is rejected. The refresh token alone authenticates the call; no `Authorization` header is required, and one sent alongside is ignored.
     * @summary Rotate refresh token and issue new access token
     * @param {RefreshTokenRequest} refreshTokenRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    refreshTokenRaw(requestParameters: RefreshTokenOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RefreshToken200Response>>;

    /**
     * Exchanges a valid refresh token for a newly issued JWT access token and a rotated refresh token. Implements strict single-use refresh token rotation: the supplied refresh token is permanently invalidated upon successful exchange. If an expired, already-rotated, or revoked token is presented, the request is rejected. The refresh token alone authenticates the call; no `Authorization` header is required, and one sent alongside is ignored.
     * Rotate refresh token and issue new access token
     */
    refreshToken(requestParameters: RefreshTokenOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RefreshToken200Response>;

    /**
     * Creates request options for revokeSession without sending the request
     * @param {string} id Unique UUID identifier of the session to revoke
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    revokeSessionRequestOpts(requestParameters: RevokeSessionRequest): Promise<runtime.RequestOpts>;

    /**
     * Immediately revokes a specific user login session by its unique session ID. All refresh tokens issued within this session are invalidated, preventing further token refreshes. If the revoked session corresponds to the current client connection, subsequent refresh attempts will fail.
     * @summary Revoke an active login session
     * @param {string} id Unique UUID identifier of the session to revoke
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthApiInterface
     */
    revokeSessionRaw(requestParameters: RevokeSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Immediately revokes a specific user login session by its unique session ID. All refresh tokens issued within this session are invalidated, preventing further token refreshes. If the revoked session corresponds to the current client connection, subsequent refresh attempts will fail.
     * Revoke an active login session
     */
    revokeSession(requestParameters: RevokeSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

}

/**
 * 
 */
export class AuthApi extends runtime.BaseAPI implements AuthApiInterface {

    /**
     * Creates request options for getMyPermissions without sending the request
     */
    async getMyPermissionsRequestOpts(requestParameters: GetMyPermissionsRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/auth/me/permissions`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the complete list of permission keys granted to the authenticated user or agent under their active project role. Call this endpoint before planning or executing multi-step schema modifications, role administration, or entity mutations to verify current operational capabilities. Returns `[\"*\"]` for project owners who possess root administrative privileges, or an array of granular permission keys (e.g. `entity.view`, `entity.create`, `template.create`, `billing.view_usage`) for assigned roles. Returns an empty array if no role is currently assigned.
     * Discover authenticated caller permissions and capabilities
     */
    async getMyPermissionsRaw(requestParameters: GetMyPermissionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetMyPermissions200Response>> {
        const requestOptions = await this.getMyPermissionsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetMyPermissions200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns the complete list of permission keys granted to the authenticated user or agent under their active project role. Call this endpoint before planning or executing multi-step schema modifications, role administration, or entity mutations to verify current operational capabilities. Returns `[\"*\"]` for project owners who possess root administrative privileges, or an array of granular permission keys (e.g. `entity.view`, `entity.create`, `template.create`, `billing.view_usage`) for assigned roles. Returns an empty array if no role is currently assigned.
     * Discover authenticated caller permissions and capabilities
     */
    async getMyPermissions(requestParameters: GetMyPermissionsRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetMyPermissions200Response> {
        const response = await this.getMyPermissionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for googleLogin without sending the request
     */
    async googleLoginRequestOpts(requestParameters: GoogleLoginOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['googleLoginRequest'] == null) {
            throw new runtime.RequiredError(
                'googleLoginRequest',
                'Required parameter "googleLoginRequest" was null or undefined when calling googleLogin().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/auth/google-login`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: GoogleLoginRequestToJSON(requestParameters['googleLoginRequest']),
        };
    }

    /**
     * Authenticates a user using a Google Identity Services (GIS) ID token. Verifies the cryptographic token signature against Google public keys. If no account exists for the verified email address, a new user account is automatically provisioned and verified. Returns a JWT access token and refresh token for the active session.
     * Authenticate or register with Google Sign-In
     */
    async googleLoginRaw(requestParameters: GoogleLoginOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GoogleLogin200Response>> {
        const requestOptions = await this.googleLoginRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GoogleLogin200ResponseFromJSON(jsonValue));
    }

    /**
     * Authenticates a user using a Google Identity Services (GIS) ID token. Verifies the cryptographic token signature against Google public keys. If no account exists for the verified email address, a new user account is automatically provisioned and verified. Returns a JWT access token and refresh token for the active session.
     * Authenticate or register with Google Sign-In
     */
    async googleLogin(requestParameters: GoogleLoginOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GoogleLogin200Response> {
        const response = await this.googleLoginRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for googleLoginRedirect without sending the request
     */
    async googleLoginRedirectRequestOpts(requestParameters: GoogleLoginRedirectRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['credential'] == null) {
            throw new runtime.RequiredError(
                'credential',
                'Required parameter "credential" was null or undefined when calling googleLoginRedirect().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        const consumes: runtime.Consume[] = [
            { contentType: 'application/x-www-form-urlencoded' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['credential'] != null) {
            formParams.append('credential', requestParameters['credential'] as any);
        }

        if (requestParameters['gCsrfToken'] != null) {
            formParams.append('g_csrf_token', requestParameters['gCsrfToken'] as any);
        }


        let urlPath = `/auth/google-login-redirect`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Handles Google Identity Services form-urlencoded POST redirection (`credential` and `g_csrf_token`). Authenticates or provisions the user account, initiates an active session, and redirects the browser (HTTP 302) to the frontend application callback URL with JWT access and refresh tokens embedded in the URL fragment.
     * Google OAuth callback redirect handler
     */
    async googleLoginRedirectRaw(requestParameters: GoogleLoginRedirectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.googleLoginRedirectRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Handles Google Identity Services form-urlencoded POST redirection (`credential` and `g_csrf_token`). Authenticates or provisions the user account, initiates an active session, and redirects the browser (HTTP 302) to the frontend application callback URL with JWT access and refresh tokens embedded in the URL fragment.
     * Google OAuth callback redirect handler
     */
    async googleLoginRedirect(requestParameters: GoogleLoginRedirectRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.googleLoginRedirectRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for listSessions without sending the request
     */
    async listSessionsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/auth/sessions`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Retrieves all login sessions recorded for the authenticated user across devices and browsers. Each session record includes client metadata (IP address, User-Agent), issuance timestamp, expiration date, current status (`active`, `expired`, `revoked`), and revocation details if applicable.
     * List active and historical user sessions
     */
    async listSessionsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListSessions200Response>> {
        const requestOptions = await this.listSessionsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListSessions200ResponseFromJSON(jsonValue));
    }

    /**
     * Retrieves all login sessions recorded for the authenticated user across devices and browsers. Each session record includes client metadata (IP address, User-Agent), issuance timestamp, expiration date, current status (`active`, `expired`, `revoked`), and revocation details if applicable.
     * List active and historical user sessions
     */
    async listSessions(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListSessions200Response> {
        const response = await this.listSessionsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for login without sending the request
     */
    async loginRequestOpts(requestParameters: LoginOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['loginRequest'] == null) {
            throw new runtime.RequiredError(
                'loginRequest',
                'Required parameter "loginRequest" was null or undefined when calling login().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/auth/login`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: LoginRequestToJSON(requestParameters['loginRequest']),
        };
    }

    /**
     * Authenticates an existing user account using email and password. Upon successful validation, generates an active login session and returns a short-lived JWT access token and a refresh token for rotating credentials.
     * Authenticate user with email and password
     */
    async loginRaw(requestParameters: LoginOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GoogleLogin200Response>> {
        const requestOptions = await this.loginRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GoogleLogin200ResponseFromJSON(jsonValue));
    }

    /**
     * Authenticates an existing user account using email and password. Upon successful validation, generates an active login session and returns a short-lived JWT access token and a refresh token for rotating credentials.
     * Authenticate user with email and password
     */
    async login(requestParameters: LoginOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GoogleLogin200Response> {
        const response = await this.loginRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for refreshToken without sending the request
     */
    async refreshTokenRequestOpts(requestParameters: RefreshTokenOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['refreshTokenRequest'] == null) {
            throw new runtime.RequiredError(
                'refreshTokenRequest',
                'Required parameter "refreshTokenRequest" was null or undefined when calling refreshToken().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/auth/refresh`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RefreshTokenRequestToJSON(requestParameters['refreshTokenRequest']),
        };
    }

    /**
     * Exchanges a valid refresh token for a newly issued JWT access token and a rotated refresh token. Implements strict single-use refresh token rotation: the supplied refresh token is permanently invalidated upon successful exchange. If an expired, already-rotated, or revoked token is presented, the request is rejected. The refresh token alone authenticates the call; no `Authorization` header is required, and one sent alongside is ignored.
     * Rotate refresh token and issue new access token
     */
    async refreshTokenRaw(requestParameters: RefreshTokenOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RefreshToken200Response>> {
        const requestOptions = await this.refreshTokenRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RefreshToken200ResponseFromJSON(jsonValue));
    }

    /**
     * Exchanges a valid refresh token for a newly issued JWT access token and a rotated refresh token. Implements strict single-use refresh token rotation: the supplied refresh token is permanently invalidated upon successful exchange. If an expired, already-rotated, or revoked token is presented, the request is rejected. The refresh token alone authenticates the call; no `Authorization` header is required, and one sent alongside is ignored.
     * Rotate refresh token and issue new access token
     */
    async refreshToken(requestParameters: RefreshTokenOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RefreshToken200Response> {
        const response = await this.refreshTokenRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for revokeSession without sending the request
     */
    async revokeSessionRequestOpts(requestParameters: RevokeSessionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling revokeSession().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/auth/sessions/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Immediately revokes a specific user login session by its unique session ID. All refresh tokens issued within this session are invalidated, preventing further token refreshes. If the revoked session corresponds to the current client connection, subsequent refresh attempts will fail.
     * Revoke an active login session
     */
    async revokeSessionRaw(requestParameters: RevokeSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.revokeSessionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Immediately revokes a specific user login session by its unique session ID. All refresh tokens issued within this session are invalidated, preventing further token refreshes. If the revoked session corresponds to the current client connection, subsequent refresh attempts will fail.
     * Revoke an active login session
     */
    async revokeSession(requestParameters: RevokeSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.revokeSessionRaw(requestParameters, initOverrides);
    }

}
