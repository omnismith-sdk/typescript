/* tslint:disable */
/* eslint-disable */
/**
 * Omnismith API
 * API Documentation for Omnismith
 *
 * The version of the OpenAPI document: 1.0.14
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AggregateEntities200Response,
  AggregateEntitiesRequest,
  BatchExecuteEntityActionRequest,
  BatchExecuteEntityActionResponse,
  BatchWriteEntitiesRequest,
  BatchWriteEntitiesResponse,
  CreateEntity201Response,
  CreateEntityRequest,
  EntityResponse,
  ErrorResponse,
  ExecuteEntityActionRequest,
  ExecuteEntityActionResponse,
  ExportEntitiesRequest,
  GetEntityChart200Response,
  GetEntityHistory200Response,
  ImportEntities200Response,
  IngestMetricsRequest,
  ListEntityActions200Response,
  ReplaceEntityRequest,
  SearchEntities200Response,
  SearchEntitiesRequest,
  SemanticSearchEntitiesRequest,
  SemanticSearchResultItem,
  UpdateEntityRequest,
  ValidationErrorResponse,
} from '../models/index';
import {
    AggregateEntities200ResponseFromJSON,
    AggregateEntities200ResponseToJSON,
    AggregateEntitiesRequestFromJSON,
    AggregateEntitiesRequestToJSON,
    BatchExecuteEntityActionRequestFromJSON,
    BatchExecuteEntityActionRequestToJSON,
    BatchExecuteEntityActionResponseFromJSON,
    BatchExecuteEntityActionResponseToJSON,
    BatchWriteEntitiesRequestFromJSON,
    BatchWriteEntitiesRequestToJSON,
    BatchWriteEntitiesResponseFromJSON,
    BatchWriteEntitiesResponseToJSON,
    CreateEntity201ResponseFromJSON,
    CreateEntity201ResponseToJSON,
    CreateEntityRequestFromJSON,
    CreateEntityRequestToJSON,
    EntityResponseFromJSON,
    EntityResponseToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ExecuteEntityActionRequestFromJSON,
    ExecuteEntityActionRequestToJSON,
    ExecuteEntityActionResponseFromJSON,
    ExecuteEntityActionResponseToJSON,
    ExportEntitiesRequestFromJSON,
    ExportEntitiesRequestToJSON,
    GetEntityChart200ResponseFromJSON,
    GetEntityChart200ResponseToJSON,
    GetEntityHistory200ResponseFromJSON,
    GetEntityHistory200ResponseToJSON,
    ImportEntities200ResponseFromJSON,
    ImportEntities200ResponseToJSON,
    IngestMetricsRequestFromJSON,
    IngestMetricsRequestToJSON,
    ListEntityActions200ResponseFromJSON,
    ListEntityActions200ResponseToJSON,
    ReplaceEntityRequestFromJSON,
    ReplaceEntityRequestToJSON,
    SearchEntities200ResponseFromJSON,
    SearchEntities200ResponseToJSON,
    SearchEntitiesRequestFromJSON,
    SearchEntitiesRequestToJSON,
    SemanticSearchEntitiesRequestFromJSON,
    SemanticSearchEntitiesRequestToJSON,
    SemanticSearchResultItemFromJSON,
    SemanticSearchResultItemToJSON,
    UpdateEntityRequestFromJSON,
    UpdateEntityRequestToJSON,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/index';

export interface AggregateEntitiesOperationRequest {
    templateId: string;
    aggregateEntitiesRequest: AggregateEntitiesRequest;
    xOmnismithProjectId?: string;
}

export interface BatchExecuteEntityActionOperationRequest {
    slug: string;
    batchExecuteEntityActionRequest: BatchExecuteEntityActionRequest;
    xOmnismithProjectId?: string;
}

export interface BatchWriteEntitiesOperationRequest {
    batchWriteEntitiesRequest: BatchWriteEntitiesRequest;
    xOmnismithProjectId?: string;
}

export interface CreateEntityOperationRequest {
    template: string;
    xOmnismithProjectId?: string;
    createEntityRequest?: CreateEntityRequest;
}

export interface DeleteEntityRequest {
    id: string;
    xOmnismithProjectId?: string;
}

export interface ExecuteEntityActionOperationRequest {
    id: string;
    slug: string;
    xOmnismithProjectId?: string;
    executeEntityActionRequest?: ExecuteEntityActionRequest;
}

export interface ExportEntitiesOperationRequest {
    templateId: string;
    exportEntitiesRequest: ExportEntitiesRequest;
    xOmnismithProjectId?: string;
    sortField?: string | null;
    sortDirection?: ExportEntitiesOperationSortDirectionEnum;
}

export interface GetEntityRequest {
    id: string;
    xOmnismithProjectId?: string;
    verbose?: boolean;
    fields?: Array<string>;
}

export interface GetEntityChartRequest {
    id: string;
    attributeIds: string;
    start: number;
    end: number;
    xOmnismithProjectId?: string;
    aggregateFunc?: GetEntityChartAggregateFuncEnum;
    bucketWidth?: GetEntityChartBucketWidthEnum;
}

export interface GetEntityHistoryRequest {
    id: string;
    xOmnismithProjectId?: string;
    page?: number;
    limit?: number;
    sortBy?: GetEntityHistorySortByEnum;
    sortDirection?: GetEntityHistorySortDirectionEnum;
    search?: string;
    attributeIds?: string;
    start?: Date;
    end?: Date;
    authorEmail?: string;
}

export interface ImportEntitiesRequest {
    templateId: string;
    file: Blob;
    xOmnismithProjectId?: string;
}

export interface IngestEntityMetricsRequest {
    id: string;
    ingestMetricsRequest: IngestMetricsRequest;
    xOmnismithProjectId?: string;
}

export interface ListEntityActionsRequest {
    id: string;
    xOmnismithProjectId?: string;
}

export interface ReplaceEntityOperationRequest {
    id: string;
    replaceEntityRequest: ReplaceEntityRequest;
    xOmnismithProjectId?: string;
}

export interface SearchEntitiesOperationRequest {
    templateId: string;
    searchEntitiesRequest: SearchEntitiesRequest;
    xOmnismithProjectId?: string;
    limit?: number;
    offset?: number;
    sortField?: string | null;
    sortDirection?: SearchEntitiesOperationSortDirectionEnum;
}

export interface SemanticSearchEntitiesOperationRequest {
    semanticSearchEntitiesRequest: SemanticSearchEntitiesRequest;
    xOmnismithProjectId?: string;
}

export interface UpdateEntityOperationRequest {
    id: string;
    updateEntityRequest: UpdateEntityRequest;
    xOmnismithProjectId?: string;
}

/**
 * EntityApi - interface
 * 
 * @export
 * @interface EntityApiInterface
 */
export interface EntityApiInterface {
    /**
     * Creates request options for aggregateEntities without sending the request
     * @param {string} templateId Template UUID or human-readable template slug
     * @param {AggregateEntitiesRequest} aggregateEntitiesRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    aggregateEntitiesRequestOpts(requestParameters: AggregateEntitiesOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Answers \"how many\", \"how much\" and \"broken down by\" questions in one call, computed by the database. Use it instead of paginating `search_entities` and tallying rows: a count or a per-status breakdown of a 10,000-record template is one small response. See `filter_groups`, `group_by` and `aggregations` for the request shape.  Each response group\'s `key` mirrors `group_by` in order (`value` is the stored value, `custom_value` is the list item label or the referenced record\'s display value; `null` groups the records that have no value for that attribute), and `aggregates` mirrors `aggregations` in order. Numbers come back as floats, dates as RFC 3339 strings, and `null` when no record in the group has a value. Metric attributes are rejected with a 400: they are time series, reduced over a time window with `get_entity_chart` instead — this endpoint reduces the current dimension values of records.  Groups are ordered by the first aggregation descending (nulls last), then by key. `limit` (1-100, default 50) caps the groups returned; `truncated: true` means more groups exist — narrow with `filter_groups` or group by fewer fields.  Read-only. Applies the caller\'s template access and row scopes exactly as search does; restricted attributes are not valid fields.
     * @summary Count, sum, average, min or max entities, optionally grouped by attributes
     * @param {string} templateId Template UUID or human-readable template slug
     * @param {AggregateEntitiesRequest} aggregateEntitiesRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    aggregateEntitiesRaw(requestParameters: AggregateEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AggregateEntities200Response>>;

    /**
     * Answers \"how many\", \"how much\" and \"broken down by\" questions in one call, computed by the database. Use it instead of paginating `search_entities` and tallying rows: a count or a per-status breakdown of a 10,000-record template is one small response. See `filter_groups`, `group_by` and `aggregations` for the request shape.  Each response group\'s `key` mirrors `group_by` in order (`value` is the stored value, `custom_value` is the list item label or the referenced record\'s display value; `null` groups the records that have no value for that attribute), and `aggregates` mirrors `aggregations` in order. Numbers come back as floats, dates as RFC 3339 strings, and `null` when no record in the group has a value. Metric attributes are rejected with a 400: they are time series, reduced over a time window with `get_entity_chart` instead — this endpoint reduces the current dimension values of records.  Groups are ordered by the first aggregation descending (nulls last), then by key. `limit` (1-100, default 50) caps the groups returned; `truncated: true` means more groups exist — narrow with `filter_groups` or group by fewer fields.  Read-only. Applies the caller\'s template access and row scopes exactly as search does; restricted attributes are not valid fields.
     * Count, sum, average, min or max entities, optionally grouped by attributes
     */
    aggregateEntities(requestParameters: AggregateEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AggregateEntities200Response>;

    /**
     * Creates request options for batchExecuteEntityAction without sending the request
     * @param {string} slug Action slug, as listed by &#x60;GET /templates/{templateId}/actions&#x60; or &#x60;GET /entities/{id}/actions&#x60;
     * @param {BatchExecuteEntityActionRequest} batchExecuteEntityActionRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    batchExecuteEntityActionRequestOpts(requestParameters: BatchExecuteEntityActionOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Runs one named action on many records in a single call — \"confirm these forty\". Each record goes through exactly what `POST /entities/{id}/actions/{slug}` (`execute_entity_action`) does: precondition, field matching, presets, type validation and the template\'s rules, with history attributed to the action. `values` are the same for every record.  The action is resolved per record on its template, so the selection may span templates that each define the slug; a record whose template does not is reported as `failed` with a 404 body.  At most 100 records per call; page a larger selection.  Outcomes: By default (`atomic: false`) every record is attempted and the response is `200` with one outcome per record — `executed` with a receipt, or `precondition_failed` / `rule_violated` / `failed` with the error body the single-record endpoint would have returned. Read the counters, then `results` for the records that did not run.  With `atomic: true` the batch runs in one transaction and the first record that does not execute rolls all of it back. That case answers with that record\'s own error status and body plus `failed_entity_id`, not with a results list.  Quotas: The dimension-update quota is checked for the whole selection before any record is written.  Errors: - `400` — the body is not the documented shape, or lists an entity twice. - `402` — the selection would cross the tier\'s update quota. - `422` — a submitted value does not fit the action (only when nothing could run).
     * @summary Execute an action on a selection of entities
     * @param {string} slug Action slug, as listed by &#x60;GET /templates/{templateId}/actions&#x60; or &#x60;GET /entities/{id}/actions&#x60;
     * @param {BatchExecuteEntityActionRequest} batchExecuteEntityActionRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    batchExecuteEntityActionRaw(requestParameters: BatchExecuteEntityActionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchExecuteEntityActionResponse>>;

    /**
     * Runs one named action on many records in a single call — \"confirm these forty\". Each record goes through exactly what `POST /entities/{id}/actions/{slug}` (`execute_entity_action`) does: precondition, field matching, presets, type validation and the template\'s rules, with history attributed to the action. `values` are the same for every record.  The action is resolved per record on its template, so the selection may span templates that each define the slug; a record whose template does not is reported as `failed` with a 404 body.  At most 100 records per call; page a larger selection.  Outcomes: By default (`atomic: false`) every record is attempted and the response is `200` with one outcome per record — `executed` with a receipt, or `precondition_failed` / `rule_violated` / `failed` with the error body the single-record endpoint would have returned. Read the counters, then `results` for the records that did not run.  With `atomic: true` the batch runs in one transaction and the first record that does not execute rolls all of it back. That case answers with that record\'s own error status and body plus `failed_entity_id`, not with a results list.  Quotas: The dimension-update quota is checked for the whole selection before any record is written.  Errors: - `400` — the body is not the documented shape, or lists an entity twice. - `402` — the selection would cross the tier\'s update quota. - `422` — a submitted value does not fit the action (only when nothing could run).
     * Execute an action on a selection of entities
     */
    batchExecuteEntityAction(requestParameters: BatchExecuteEntityActionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchExecuteEntityActionResponse>;

    /**
     * Creates request options for batchWriteEntities without sending the request
     * @param {BatchWriteEntitiesRequest} batchWriteEntitiesRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    batchWriteEntitiesRequestOpts(requestParameters: BatchWriteEntitiesOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Applies an ordered, heterogeneous list of entity writes in a single call: update these twenty, create three, replace two, delete one.  This is distinct from CSV import, which moves a homogeneous set of new rows. Use this endpoint when the set of edits is already computed and addresses known records.  Operations: Every entry names an `op` and carries exactly the fields for it — nothing more, nothing less: - `create`  — `{ \"op\": \"create\", \"template\": \"<slug|uuid>\", \"id\"?: \"<uuidv7>\", \"attributes\": { ... } }` (`attributes` may be `{}`) - `update`  — `{ \"op\": \"update\", \"id\": \"<uuid>\", \"attributes\": { ... } }` (non-empty; partial, like PATCH) - `replace` — `{ \"op\": \"replace\", \"id\": \"<uuid>\", \"attributes\": { ... } }` (like PUT: attributes absent from the map are cleared; `{}` clears all; metrics rejected) - `delete`  — `{ \"op\": \"delete\", \"id\": \"<uuid>\" }` (soft delete)  `id` always means the entity id; `template` always means the template slug or UUID. See `BatchOperationInput`\'s `attributes` property for the accepted value shapes.  At most 100 operations per call. Larger sets must be split.  Failure handling: By default (`atomic: false`) every operation is attempted, successes stand, and each failure is reported against its index with the same error body the single-entity endpoint would have returned. The response is `200` regardless of how many entries failed; read `failed` and the per-item `status`.  With `atomic: true` the whole batch runs in one transaction and the first failure rolls all of it back. That case answers with the failing operation\'s own error status and body plus `failed_index`, not with a results list. Atomic batches reject metric attribute values, because metric telemetry is published outside the transaction and cannot be rolled back.  Quotas: Tier quotas are evaluated for the whole batch before any of it is applied, so a batch that would cross the limit is refused as a unit rather than applied halfway.
     * @summary Apply a batch of mixed entity creates, updates, replaces, and deletes
     * @param {BatchWriteEntitiesRequest} batchWriteEntitiesRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    batchWriteEntitiesRaw(requestParameters: BatchWriteEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchWriteEntitiesResponse>>;

    /**
     * Applies an ordered, heterogeneous list of entity writes in a single call: update these twenty, create three, replace two, delete one.  This is distinct from CSV import, which moves a homogeneous set of new rows. Use this endpoint when the set of edits is already computed and addresses known records.  Operations: Every entry names an `op` and carries exactly the fields for it — nothing more, nothing less: - `create`  — `{ \"op\": \"create\", \"template\": \"<slug|uuid>\", \"id\"?: \"<uuidv7>\", \"attributes\": { ... } }` (`attributes` may be `{}`) - `update`  — `{ \"op\": \"update\", \"id\": \"<uuid>\", \"attributes\": { ... } }` (non-empty; partial, like PATCH) - `replace` — `{ \"op\": \"replace\", \"id\": \"<uuid>\", \"attributes\": { ... } }` (like PUT: attributes absent from the map are cleared; `{}` clears all; metrics rejected) - `delete`  — `{ \"op\": \"delete\", \"id\": \"<uuid>\" }` (soft delete)  `id` always means the entity id; `template` always means the template slug or UUID. See `BatchOperationInput`\'s `attributes` property for the accepted value shapes.  At most 100 operations per call. Larger sets must be split.  Failure handling: By default (`atomic: false`) every operation is attempted, successes stand, and each failure is reported against its index with the same error body the single-entity endpoint would have returned. The response is `200` regardless of how many entries failed; read `failed` and the per-item `status`.  With `atomic: true` the whole batch runs in one transaction and the first failure rolls all of it back. That case answers with the failing operation\'s own error status and body plus `failed_index`, not with a results list. Atomic batches reject metric attribute values, because metric telemetry is published outside the transaction and cannot be rolled back.  Quotas: Tier quotas are evaluated for the whole batch before any of it is applied, so a batch that would cross the limit is refused as a unit rather than applied halfway.
     * Apply a batch of mixed entity creates, updates, replaces, and deletes
     */
    batchWriteEntities(requestParameters: BatchWriteEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchWriteEntitiesResponse>;

    /**
     * Creates request options for createEntity without sending the request
     * @param {string} template Template UUID or human-readable slug
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {CreateEntityRequest} [createEntityRequest] 
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    createEntityRequestOpts(requestParameters: CreateEntityOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Creates one entity of the template named in the path — `{template}` is the template\'s slug or UUID — and returns its id. Pass `id` to choose the entity\'s UUIDv7 yourself (cross-system keys); otherwise one is generated. An `id` that already exists — even a soft-deleted entity\'s — is rejected with `409` rather than overwritten: if a retry might be hitting this because an earlier call\'s response was lost, `GET /entities/{id}` first to check whether it already carries what you meant to write, rather than retrying blindly.  See the `attributes` property for the accepted value shapes.  `attributes` is required; send `{}` to create an entity with no values yet. Metric attributes in the map are appended to the entity\'s time series; everything else becomes the entity\'s initial state and is recorded in its history.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no template with that slug or UUID in this project. - `409` — the given `id` already exists.
     * @summary Create a new dynamic entity
     * @param {string} template Template UUID or human-readable slug
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {CreateEntityRequest} [createEntityRequest] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    createEntityRaw(requestParameters: CreateEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreateEntity201Response>>;

    /**
     * Creates one entity of the template named in the path — `{template}` is the template\'s slug or UUID — and returns its id. Pass `id` to choose the entity\'s UUIDv7 yourself (cross-system keys); otherwise one is generated. An `id` that already exists — even a soft-deleted entity\'s — is rejected with `409` rather than overwritten: if a retry might be hitting this because an earlier call\'s response was lost, `GET /entities/{id}` first to check whether it already carries what you meant to write, rather than retrying blindly.  See the `attributes` property for the accepted value shapes.  `attributes` is required; send `{}` to create an entity with no values yet. Metric attributes in the map are appended to the entity\'s time series; everything else becomes the entity\'s initial state and is recorded in its history.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no template with that slug or UUID in this project. - `409` — the given `id` already exists.
     * Create a new dynamic entity
     */
    createEntity(requestParameters: CreateEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreateEntity201Response>;

    /**
     * Creates request options for deleteEntity without sending the request
     * @param {string} id Unique entity identifier (UUID) to soft-delete
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    deleteEntityRequestOpts(requestParameters: DeleteEntityRequest): Promise<runtime.RequestOpts>;

    /**
     * Marks an entity record as soft-deleted by setting its `deleted_at` timestamp.  Soft-deleted entities are immediately excluded from standard entity searches, BI row queries, and direct retrieval endpoints. Associated historical change logs and time-series telemetry remain preserved for audit compliance.
     * @summary Soft-delete an entity record
     * @param {string} id Unique entity identifier (UUID) to soft-delete
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    deleteEntityRaw(requestParameters: DeleteEntityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Marks an entity record as soft-deleted by setting its `deleted_at` timestamp.  Soft-deleted entities are immediately excluded from standard entity searches, BI row queries, and direct retrieval endpoints. Associated historical change logs and time-series telemetry remain preserved for audit compliance.
     * Soft-delete an entity record
     */
    deleteEntity(requestParameters: DeleteEntityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * Creates request options for executeEntityAction without sending the request
     * @param {string} id Unique entity identifier (UUID)
     * @param {string} slug Action slug, as listed by &#x60;GET /entities/{id}/actions&#x60;
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {ExecuteEntityActionRequest} [executeEntityActionRequest] 
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    executeEntityActionRequestOpts(requestParameters: ExecuteEntityActionOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Runs one named action on one record as a single atomic write. Prefer this over a plain update (`PATCH /entities/{id}`, the `update_entity` tool) whenever `GET /entities/{id}/actions` (`list_entity_actions`) lists an action for what you intend: the action\'s presets are applied for you and its required fields are enforced.  What happens: 1. The action\'s precondition is checked against the record\'s current values — `409` if it does not hold, with the reason. 2. `values` are matched to the action\'s `fields`; an attribute the action does not ask for, or an empty required field, is `422` keyed by `attributes.<slug>`. 3. Presets are merged on top of `values` (presets win) and the result is written exactly like an entity update: attribute types are validated and the template\'s rules are enforced (`422` on a violation, in the same `attributes.<slug>` shape).  The response is a receipt naming what was written — for a number field sent as `{ \"op\": \"increment\", \"value\": n }` that is the operation itself; the resolved number lands in the record and its history. History rows produced by the write carry the action\'s slug.  Errors: - `400` — the body is not the documented shape. - `403` — the caller may not edit this record. - `404` — no such entity, or the template has no enabled action with this slug. - `409` — the precondition does not hold; `detail` says which attribute and why. - `422` — a required field is empty, a value fails its attribute type, or a rule refuses the write.
     * @summary Execute an action on an entity
     * @param {string} id Unique entity identifier (UUID)
     * @param {string} slug Action slug, as listed by &#x60;GET /entities/{id}/actions&#x60;
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {ExecuteEntityActionRequest} [executeEntityActionRequest] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    executeEntityActionRaw(requestParameters: ExecuteEntityActionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExecuteEntityActionResponse>>;

    /**
     * Runs one named action on one record as a single atomic write. Prefer this over a plain update (`PATCH /entities/{id}`, the `update_entity` tool) whenever `GET /entities/{id}/actions` (`list_entity_actions`) lists an action for what you intend: the action\'s presets are applied for you and its required fields are enforced.  What happens: 1. The action\'s precondition is checked against the record\'s current values — `409` if it does not hold, with the reason. 2. `values` are matched to the action\'s `fields`; an attribute the action does not ask for, or an empty required field, is `422` keyed by `attributes.<slug>`. 3. Presets are merged on top of `values` (presets win) and the result is written exactly like an entity update: attribute types are validated and the template\'s rules are enforced (`422` on a violation, in the same `attributes.<slug>` shape).  The response is a receipt naming what was written — for a number field sent as `{ \"op\": \"increment\", \"value\": n }` that is the operation itself; the resolved number lands in the record and its history. History rows produced by the write carry the action\'s slug.  Errors: - `400` — the body is not the documented shape. - `403` — the caller may not edit this record. - `404` — no such entity, or the template has no enabled action with this slug. - `409` — the precondition does not hold; `detail` says which attribute and why. - `422` — a required field is empty, a value fails its attribute type, or a rule refuses the write.
     * Execute an action on an entity
     */
    executeEntityAction(requestParameters: ExecuteEntityActionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExecuteEntityActionResponse>;

    /**
     * Creates request options for exportEntities without sending the request
     * @param {string} templateId Unique identifier (UUID) of the template schema to export
     * @param {ExportEntitiesRequest} exportEntitiesRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {string} [sortField] Attribute UUID, attribute slug, or standard field (id, created_at, updated_at, deleted_at) to sort by
     * @param {'asc' | 'desc'} [sortDirection] Sort direction: \&quot;asc\&quot; (ascending) or \&quot;desc\&quot; (descending)
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    exportEntitiesRequestOpts(requestParameters: ExportEntitiesOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Exports entity records of a template schema matching filter criteria as a streaming CSV download.  ### Re-importable CSV Schema Format The generated CSV conforms to the Omnismith two-row metadata specification, making it directly compatible with `POST /entities/import/{template_id}`: - **Row 1**: Display column names and attribute aliases. - **Row 2**: Metadata row prefixed with `#` containing attribute UUIDs and column IDs (e.g. `#id`, `#018b2f1b-8c1a...`). - **Row 3+**: Entity data records.  Accepts the same `filter_groups` and `global_search` payload as `searchEntities`.  ### Filters (`filter_groups`) A list of groups; clauses inside a group are AND-ed, groups are OR-ed. `[[a, b]]` is `a AND b`; `[[a], [b, c]]` is `a OR (b AND c)`; `[]` applies no filter. ```json [   [     {\"field\": \"status\", \"operator\": \"in\", \"value\": [\"018b…0020\", \"018b…0021\"]},     {\"field\": \"created_at\", \"operator\": \"between\", \"value\": [\"2026-01-01\", \"2026-03-31\"]},     {\"field\": \"customer.tier\", \"operator\": \"eq\", \"value\": \"018b…0042\"}   ],   [{\"field\": \"priority\", \"operator\": \"eq\", \"value\": \"018b…0007\"}] ] ``` - **`field`**: attribute slug or UUID, a standard field (`id`, `created_at`, `updated_at`), or a one-hop path `<reference>.<attribute>` that filters on an attribute of the referenced record (e.g. `customer.tier`). One hop only. - **`operator`** and **`value`**: `eq`, `neq`, `gt`, `lt`, `like` (case-insensitive substring), `not-like` take a string; `in`, `not-in` take a non-empty list of strings; `between` takes `[lower, upper]` (inclusive; number, date, datetime attributes and `created_at` / `updated_at`); `empty`, `not-empty` take no value. - List and reference attributes compare the stored id (from `list_item_ids` / `reference_entity_ids` or the schema), never the label. - Unknown fields, operators that do not fit the field, malformed values and paths that do not traverse a reference are refused with 400 naming the valid fields; a path into a template the caller may not view is 403.  ### Sorting Sort results via `sort_field` (attribute UUID, slug, or standard timestamp) and `sort_direction` (`asc`/`desc`).
     * @summary Export entities to structured CSV file
     * @param {string} templateId Unique identifier (UUID) of the template schema to export
     * @param {ExportEntitiesRequest} exportEntitiesRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {string} [sortField] Attribute UUID, attribute slug, or standard field (id, created_at, updated_at, deleted_at) to sort by
     * @param {'asc' | 'desc'} [sortDirection] Sort direction: \&quot;asc\&quot; (ascending) or \&quot;desc\&quot; (descending)
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    exportEntitiesRaw(requestParameters: ExportEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>>;

    /**
     * Exports entity records of a template schema matching filter criteria as a streaming CSV download.  ### Re-importable CSV Schema Format The generated CSV conforms to the Omnismith two-row metadata specification, making it directly compatible with `POST /entities/import/{template_id}`: - **Row 1**: Display column names and attribute aliases. - **Row 2**: Metadata row prefixed with `#` containing attribute UUIDs and column IDs (e.g. `#id`, `#018b2f1b-8c1a...`). - **Row 3+**: Entity data records.  Accepts the same `filter_groups` and `global_search` payload as `searchEntities`.  ### Filters (`filter_groups`) A list of groups; clauses inside a group are AND-ed, groups are OR-ed. `[[a, b]]` is `a AND b`; `[[a], [b, c]]` is `a OR (b AND c)`; `[]` applies no filter. ```json [   [     {\"field\": \"status\", \"operator\": \"in\", \"value\": [\"018b…0020\", \"018b…0021\"]},     {\"field\": \"created_at\", \"operator\": \"between\", \"value\": [\"2026-01-01\", \"2026-03-31\"]},     {\"field\": \"customer.tier\", \"operator\": \"eq\", \"value\": \"018b…0042\"}   ],   [{\"field\": \"priority\", \"operator\": \"eq\", \"value\": \"018b…0007\"}] ] ``` - **`field`**: attribute slug or UUID, a standard field (`id`, `created_at`, `updated_at`), or a one-hop path `<reference>.<attribute>` that filters on an attribute of the referenced record (e.g. `customer.tier`). One hop only. - **`operator`** and **`value`**: `eq`, `neq`, `gt`, `lt`, `like` (case-insensitive substring), `not-like` take a string; `in`, `not-in` take a non-empty list of strings; `between` takes `[lower, upper]` (inclusive; number, date, datetime attributes and `created_at` / `updated_at`); `empty`, `not-empty` take no value. - List and reference attributes compare the stored id (from `list_item_ids` / `reference_entity_ids` or the schema), never the label. - Unknown fields, operators that do not fit the field, malformed values and paths that do not traverse a reference are refused with 400 naming the valid fields; a path into a template the caller may not view is 403.  ### Sorting Sort results via `sort_field` (attribute UUID, slug, or standard timestamp) and `sort_direction` (`asc`/`desc`).
     * Export entities to structured CSV file
     */
    exportEntities(requestParameters: ExportEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob>;

    /**
     * Creates request options for getEntity without sending the request
     * @param {string} id Unique entity identifier (UUID)
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {boolean} [verbose] When true, attribute_values is an array of EntityAttributeValue items with attribute id, slug, raw value, resolved custom_value and reference_entity_id. When false (default), attribute_values is a compact object mapping attribute slug to display value, with the ids behind list, reference and file labels in list_item_ids, reference_entity_ids and file_ids.
     * @param {Array<string>} [fields] Attribute slugs or UUIDs to project, e.g. [\&quot;title\&quot;, \&quot;status\&quot;]. Standard fields (id, template_id, template_slug, created_at, updated_at) are always included and do not need to be listed. When specified, only the requested attributes are fetched and returned in attribute_values, avoiding database hydration for unneeded attributes and significantly reducing response payload size. If omitted, all attributes defined on the template are returned.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    getEntityRequestOpts(requestParameters: GetEntityRequest): Promise<runtime.RequestOpts>;

    /**
     * Retrieves the full hydrated record for a dynamic entity by its unique identifier (UUID). See `verbose` and `fields` for response shape and projection options.
     * @summary Get an entity record by ID
     * @param {string} id Unique entity identifier (UUID)
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {boolean} [verbose] When true, attribute_values is an array of EntityAttributeValue items with attribute id, slug, raw value, resolved custom_value and reference_entity_id. When false (default), attribute_values is a compact object mapping attribute slug to display value, with the ids behind list, reference and file labels in list_item_ids, reference_entity_ids and file_ids.
     * @param {Array<string>} [fields] Attribute slugs or UUIDs to project, e.g. [\&quot;title\&quot;, \&quot;status\&quot;]. Standard fields (id, template_id, template_slug, created_at, updated_at) are always included and do not need to be listed. When specified, only the requested attributes are fetched and returned in attribute_values, avoiding database hydration for unneeded attributes and significantly reducing response payload size. If omitted, all attributes defined on the template are returned.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    getEntityRaw(requestParameters: GetEntityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityResponse>>;

    /**
     * Retrieves the full hydrated record for a dynamic entity by its unique identifier (UUID). See `verbose` and `fields` for response shape and projection options.
     * Get an entity record by ID
     */
    getEntity(requestParameters: GetEntityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityResponse>;

    /**
     * Creates request options for getEntityChart without sending the request
     * @param {string} id Unique entity identifier (UUID)
     * @param {string} attributeIds Comma-separated metric attribute UUIDs to aggregate
     * @param {number} start Start timestamp as Unix epoch in seconds
     * @param {number} end End timestamp as Unix epoch in seconds
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {'sum' | 'avg' | 'min' | 'max' | 'count' | 'first' | 'last'} [aggregateFunc] Aggregation function applied within each bucket
     * @param {'1 second' | '5 seconds' | '10 seconds' | '1 minute' | '5 minutes' | '10 minutes' | '15 minutes' | '30 minutes' | '1 hour' | '6 hours' | '12 hours' | '1 day' | '1 week' | '1 month'} [bucketWidth] Time bucket width interval (e.g. 1 minute, 5 minutes, 1 hour, 1 day)
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    getEntityChartRequestOpts(requestParameters: GetEntityChartRequest): Promise<runtime.RequestOpts>;

    /**
     * Retrieves aggregated, time-bucketed metric time-series data for an entity. Returns one `series` entry per requested `attribute_id`, each a chronological list of `{time, value}` points aggregated at the given `bucket_width`.
     * @summary Get entity chart time-series data
     * @param {string} id Unique entity identifier (UUID)
     * @param {string} attributeIds Comma-separated metric attribute UUIDs to aggregate
     * @param {number} start Start timestamp as Unix epoch in seconds
     * @param {number} end End timestamp as Unix epoch in seconds
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {'sum' | 'avg' | 'min' | 'max' | 'count' | 'first' | 'last'} [aggregateFunc] Aggregation function applied within each bucket
     * @param {'1 second' | '5 seconds' | '10 seconds' | '1 minute' | '5 minutes' | '10 minutes' | '15 minutes' | '30 minutes' | '1 hour' | '6 hours' | '12 hours' | '1 day' | '1 week' | '1 month'} [bucketWidth] Time bucket width interval (e.g. 1 minute, 5 minutes, 1 hour, 1 day)
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    getEntityChartRaw(requestParameters: GetEntityChartRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetEntityChart200Response>>;

    /**
     * Retrieves aggregated, time-bucketed metric time-series data for an entity. Returns one `series` entry per requested `attribute_id`, each a chronological list of `{time, value}` points aggregated at the given `bucket_width`.
     * Get entity chart time-series data
     */
    getEntityChart(requestParameters: GetEntityChartRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetEntityChart200Response>;

    /**
     * Creates request options for getEntityHistory without sending the request
     * @param {string} id Unique entity identifier (UUID)
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {number} [page] 1-based page number for pagination
     * @param {number} [limit] Number of history records per page (1-100)
     * @param {'created_at' | 'attribute_id' | 'value'} [sortBy] Field to sort change logs by
     * @param {'asc' | 'desc'} [sortDirection] Sort direction: \&quot;asc\&quot; (ascending) or \&quot;desc\&quot; (descending)
     * @param {string} [search] Free-text search filter matching against old and new attribute values
     * @param {string} [attributeIds] Comma-separated attribute UUIDs to filter change history
     * @param {Date} [start] Filter change records occurring on or after this timestamp (ISO 8601 or YYYY-MM-DD HH:MM:SS format)
     * @param {Date} [end] Filter change records occurring on or before this timestamp (ISO 8601 or YYYY-MM-DD HH:MM:SS format)
     * @param {string} [authorEmail] Filter change records by author or actor email
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    getEntityHistoryRequestOpts(requestParameters: GetEntityHistoryRequest): Promise<runtime.RequestOpts>;

    /**
     * Retrieves the immutable change audit log for an entity\'s dimension attribute mutations. Records mutations to dimension, list, and reference attribute values; metric telemetry is excluded and lives in time-series storage instead (see `get_entity_chart`). Returns paginated `items` (each an attribute change from `old_value` to `value`, with `author_email` and `action_slug` when the write came from an action) plus `total` matching records.
     * @summary Get entity dimension change history
     * @param {string} id Unique entity identifier (UUID)
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {number} [page] 1-based page number for pagination
     * @param {number} [limit] Number of history records per page (1-100)
     * @param {'created_at' | 'attribute_id' | 'value'} [sortBy] Field to sort change logs by
     * @param {'asc' | 'desc'} [sortDirection] Sort direction: \&quot;asc\&quot; (ascending) or \&quot;desc\&quot; (descending)
     * @param {string} [search] Free-text search filter matching against old and new attribute values
     * @param {string} [attributeIds] Comma-separated attribute UUIDs to filter change history
     * @param {Date} [start] Filter change records occurring on or after this timestamp (ISO 8601 or YYYY-MM-DD HH:MM:SS format)
     * @param {Date} [end] Filter change records occurring on or before this timestamp (ISO 8601 or YYYY-MM-DD HH:MM:SS format)
     * @param {string} [authorEmail] Filter change records by author or actor email
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    getEntityHistoryRaw(requestParameters: GetEntityHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetEntityHistory200Response>>;

    /**
     * Retrieves the immutable change audit log for an entity\'s dimension attribute mutations. Records mutations to dimension, list, and reference attribute values; metric telemetry is excluded and lives in time-series storage instead (see `get_entity_chart`). Returns paginated `items` (each an attribute change from `old_value` to `value`, with `author_email` and `action_slug` when the write came from an action) plus `total` matching records.
     * Get entity dimension change history
     */
    getEntityHistory(requestParameters: GetEntityHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetEntityHistory200Response>;

    /**
     * Creates request options for importEntities without sending the request
     * @param {string} templateId Unique identifier (UUID) of the template schema to import entities into
     * @param {Blob} file CSV file exported from the export endpoint or matching its format
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    importEntitiesRequestOpts(requestParameters: ImportEntitiesRequest): Promise<runtime.RequestOpts>;

    /**
     * Bulk imports entity records into a template schema from a structured CSV file.  ### Upsert Semantics - **Update existing**: If a data row includes an `id` matching an existing entity UUID, that entity is updated. - **Create new**: If the `id` column is empty, omitted, or contains a new UUID, a new entity record is created.  ### Required 2-Row CSV Header Format The CSV file must follow the Omnismith two-row header format (identical to the output of `POST /entities/export/{template_id}`): - **Row 1 (Display Header)**: Human-readable attribute names or aliases (e.g. `ID`, `SKU`, `Price`, `Category`). - **Row 2 (Metadata Marker)**: Canonical attribute identifiers prefixed by `#` (e.g. `#id`, `#018b2f1b-8c1a...`, `#018b2f1b-8c1b...`). - **Row 3+ (Data Rows)**: Serialized entity values conforming to the template\'s attribute data types.  ### Attribute Value Validation - List attributes require valid `ListItem` option UUIDs. - Reference attributes require existing target `Entity` UUIDs. - Number/Date/Boolean fields must match required format syntax.  ### Execution Summary Returns an execution report detailing counts of created, updated, skipped, and failed rows, along with granular row/column error messages.
     * @summary Import entities from structured CSV file
     * @param {string} templateId Unique identifier (UUID) of the template schema to import entities into
     * @param {Blob} file CSV file exported from the export endpoint or matching its format
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    importEntitiesRaw(requestParameters: ImportEntitiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ImportEntities200Response>>;

    /**
     * Bulk imports entity records into a template schema from a structured CSV file.  ### Upsert Semantics - **Update existing**: If a data row includes an `id` matching an existing entity UUID, that entity is updated. - **Create new**: If the `id` column is empty, omitted, or contains a new UUID, a new entity record is created.  ### Required 2-Row CSV Header Format The CSV file must follow the Omnismith two-row header format (identical to the output of `POST /entities/export/{template_id}`): - **Row 1 (Display Header)**: Human-readable attribute names or aliases (e.g. `ID`, `SKU`, `Price`, `Category`). - **Row 2 (Metadata Marker)**: Canonical attribute identifiers prefixed by `#` (e.g. `#id`, `#018b2f1b-8c1a...`, `#018b2f1b-8c1b...`). - **Row 3+ (Data Rows)**: Serialized entity values conforming to the template\'s attribute data types.  ### Attribute Value Validation - List attributes require valid `ListItem` option UUIDs. - Reference attributes require existing target `Entity` UUIDs. - Number/Date/Boolean fields must match required format syntax.  ### Execution Summary Returns an execution report detailing counts of created, updated, skipped, and failed rows, along with granular row/column error messages.
     * Import entities from structured CSV file
     */
    importEntities(requestParameters: ImportEntitiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ImportEntities200Response>;

    /**
     * Creates request options for ingestEntityMetrics without sending the request
     * @param {string} id Unique entity identifier (UUID)
     * @param {IngestMetricsRequest} ingestMetricsRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    ingestEntityMetricsRequestOpts(requestParameters: IngestEntityMetricsRequest): Promise<runtime.RequestOpts>;

    /**
     * Ingests time-series metric observations for an entity record.  Batch Telemetry Ingestion: Accepts a batch array of metric observations (`metric_values`). Each observation targets a metric attribute by `attribute_id` (UUID) or `attribute_slug` and specifies a numeric `value`.  Ingestion is asynchronous: a call is accepted immediately (`202`) and each observation is persisted shortly after, so it may not be visible in `get_entity_chart` the instant this call returns.  Strict Metric Attribute Constraint: Only attributes defined with `attribute_type: Metric` are accepted by this endpoint. Mutations to dimension, list, or reference attributes must use `PATCH /entities/{id}` instead.
     * @summary Ingest high-frequency metric observations for an entity
     * @param {string} id Unique entity identifier (UUID)
     * @param {IngestMetricsRequest} ingestMetricsRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    ingestEntityMetricsRaw(requestParameters: IngestEntityMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Ingests time-series metric observations for an entity record.  Batch Telemetry Ingestion: Accepts a batch array of metric observations (`metric_values`). Each observation targets a metric attribute by `attribute_id` (UUID) or `attribute_slug` and specifies a numeric `value`.  Ingestion is asynchronous: a call is accepted immediately (`202`) and each observation is persisted shortly after, so it may not be visible in `get_entity_chart` the instant this call returns.  Strict Metric Attribute Constraint: Only attributes defined with `attribute_type: Metric` are accepted by this endpoint. Mutations to dimension, list, or reference attributes must use `PATCH /entities/{id}` instead.
     * Ingest high-frequency metric observations for an entity
     */
    ingestEntityMetrics(requestParameters: IngestEntityMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * Creates request options for listEntityActions without sending the request
     * @param {string} id Unique entity identifier (UUID)
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    listEntityActionsRequestOpts(requestParameters: ListEntityActionsRequest): Promise<runtime.RequestOpts>;

    /**
     * The enabled actions of the entity\'s template, each evaluated against the record\'s current values.  Call this before changing a record: when an action exists for what you intend (a status transition, a hand-off), run it with `POST /entities/{id}/actions/{slug}` (the `execute_entity_action` tool) instead of a plain update (`update_entity`), so its presets and required fields apply.  Each entry says whether the action is `available` now and, if not, `unavailable_reason` names the attribute, the expectation and the current value. `fields` are the values to submit (keyed by `slug` in the execute body), with the list choices and reference target resolved; `presets` are what the action will set on its own.  Disabled actions are not listed. Read-only: nothing is written.
     * @summary List the actions available on an entity
     * @param {string} id Unique entity identifier (UUID)
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    listEntityActionsRaw(requestParameters: ListEntityActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListEntityActions200Response>>;

    /**
     * The enabled actions of the entity\'s template, each evaluated against the record\'s current values.  Call this before changing a record: when an action exists for what you intend (a status transition, a hand-off), run it with `POST /entities/{id}/actions/{slug}` (the `execute_entity_action` tool) instead of a plain update (`update_entity`), so its presets and required fields apply.  Each entry says whether the action is `available` now and, if not, `unavailable_reason` names the attribute, the expectation and the current value. `fields` are the values to submit (keyed by `slug` in the execute body), with the list choices and reference target resolved; `presets` are what the action will set on its own.  Disabled actions are not listed. Read-only: nothing is written.
     * List the actions available on an entity
     */
    listEntityActions(requestParameters: ListEntityActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListEntityActions200Response>;

    /**
     * Creates request options for replaceEntity without sending the request
     * @param {string} id Unique entity identifier (UUID)
     * @param {ReplaceEntityRequest} replaceEntityRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    replaceEntityRequestOpts(requestParameters: ReplaceEntityOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Full replacement: after this call the entity\'s dimension attributes are exactly the map you sent. Every non-metric attribute of the template that is **absent from the map is cleared**. Use PATCH unless you really mean \"make the record look exactly like this\".  ### `attributes` An object keyed by attribute **slug or UUID** — mix them freely. Each value is a plain scalar, a backfill object `{ \"value\": ..., \"updated_at\": \"<RFC 3339>\" }`, or an operation object `{ \"op\": \"increment\", \"value\": <number> }`:  ```json {   \"hostname\": \"edge-fra-01\",   \"cpu_cores\": 8,   \"is_active\": true,   \"notes\": null,   \"01a094f1-24be-7154-a5bd-3b5c33c930fb\": \"01a094f1-4c1d-7498-b73b-48ae46da900b\",   \"operational_status\": { \"value\": \"Active\", \"updated_at\": \"2026-09-12T12:23:52Z\" },   \"restart_count\": { \"op\": \"increment\", \"value\": 1 } } ```  `null` clears an attribute. `{ \"op\": \"increment\", \"value\": n }` adds `n` to the stored number without you reading it first (number attributes and metrics only; concurrent increments never lose an update). Every attribute must belong to the entity\'s template and may appear only once (the same attribute as slug *and* UUID is rejected). Full value rules are on the `EntityAttributesInput` schema.  `attributes` is required; an explicit `{}` clears every non-metric attribute. Metric attributes are append-only telemetry and cannot be replaced — including one in the map is a `422`; send it via PATCH or `/entities/{id}/metrics`.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no entity with that id.
     * @summary Replace all non-metric attributes of an entity
     * @param {string} id Unique entity identifier (UUID)
     * @param {ReplaceEntityRequest} replaceEntityRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    replaceEntityRaw(requestParameters: ReplaceEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Full replacement: after this call the entity\'s dimension attributes are exactly the map you sent. Every non-metric attribute of the template that is **absent from the map is cleared**. Use PATCH unless you really mean \"make the record look exactly like this\".  ### `attributes` An object keyed by attribute **slug or UUID** — mix them freely. Each value is a plain scalar, a backfill object `{ \"value\": ..., \"updated_at\": \"<RFC 3339>\" }`, or an operation object `{ \"op\": \"increment\", \"value\": <number> }`:  ```json {   \"hostname\": \"edge-fra-01\",   \"cpu_cores\": 8,   \"is_active\": true,   \"notes\": null,   \"01a094f1-24be-7154-a5bd-3b5c33c930fb\": \"01a094f1-4c1d-7498-b73b-48ae46da900b\",   \"operational_status\": { \"value\": \"Active\", \"updated_at\": \"2026-09-12T12:23:52Z\" },   \"restart_count\": { \"op\": \"increment\", \"value\": 1 } } ```  `null` clears an attribute. `{ \"op\": \"increment\", \"value\": n }` adds `n` to the stored number without you reading it first (number attributes and metrics only; concurrent increments never lose an update). Every attribute must belong to the entity\'s template and may appear only once (the same attribute as slug *and* UUID is rejected). Full value rules are on the `EntityAttributesInput` schema.  `attributes` is required; an explicit `{}` clears every non-metric attribute. Metric attributes are append-only telemetry and cannot be replaced — including one in the map is a `422`; send it via PATCH or `/entities/{id}/metrics`.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no entity with that id.
     * Replace all non-metric attributes of an entity
     */
    replaceEntity(requestParameters: ReplaceEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * Creates request options for searchEntities without sending the request
     * @param {string} templateId Template UUID or human-readable template slug
     * @param {SearchEntitiesRequest} searchEntitiesRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {number} [limit] Maximum number of entity records to return (1-100)
     * @param {number} [offset] Zero-based pagination offset
     * @param {string} [sortField] Attribute UUID, attribute slug, or standard field (id, created_at, updated_at, deleted_at) to sort by
     * @param {'asc' | 'desc'} [sortDirection] Sort direction: \&quot;asc\&quot; (ascending) or \&quot;desc\&quot; (descending)
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    searchEntitiesRequestOpts(requestParameters: SearchEntitiesOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Executes structured queries, full-text search, and sorting across dynamic entities of a template (`template_id` accepts a UUID or a human-readable slug). See `filter_groups`, `global_search`, `verbose`, `fields`, `sort_field` and `sort_direction` for the query and response-shape options. Use `aggregate_entities` instead of paginating through results when you only need counts, sums, or other per-group reduces.
     * @summary Search entities with filtering, sorting, and pagination
     * @param {string} templateId Template UUID or human-readable template slug
     * @param {SearchEntitiesRequest} searchEntitiesRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {number} [limit] Maximum number of entity records to return (1-100)
     * @param {number} [offset] Zero-based pagination offset
     * @param {string} [sortField] Attribute UUID, attribute slug, or standard field (id, created_at, updated_at, deleted_at) to sort by
     * @param {'asc' | 'desc'} [sortDirection] Sort direction: \&quot;asc\&quot; (ascending) or \&quot;desc\&quot; (descending)
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    searchEntitiesRaw(requestParameters: SearchEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SearchEntities200Response>>;

    /**
     * Executes structured queries, full-text search, and sorting across dynamic entities of a template (`template_id` accepts a UUID or a human-readable slug). See `filter_groups`, `global_search`, `verbose`, `fields`, `sort_field` and `sort_direction` for the query and response-shape options. Use `aggregate_entities` instead of paginating through results when you only need counts, sums, or other per-group reduces.
     * Search entities with filtering, sorting, and pagination
     */
    searchEntities(requestParameters: SearchEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SearchEntities200Response>;

    /**
     * Creates request options for semanticSearchEntities without sending the request
     * @param {SemanticSearchEntitiesRequest} semanticSearchEntitiesRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    semanticSearchEntitiesRequestOpts(requestParameters: SemanticSearchEntitiesOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Executes an approximate nearest neighbors (ANN) vector similarity search across entity dimension embeddings.  ### 768-Dimensional Embedding Vectors Requires a 768-dimensional float embedding array (`query_vector`) representing the query text or multimodal vector (e.g. generated by Google `text-embedding-004` or similar models).  ### Scoping & Filtering (`template_id`) Pass an optional `template_id` (UUID) or template slug to constrain the semantic search to records belonging to a specific template schema.  ### Cosine Similarity Threshold & Ranking (`threshold`) - `threshold`: Minimum cosine similarity score threshold (range `0.0` to `1.0`, default `0.5`). Observations below this similarity cutoff are discarded. - Matches are returned strictly ranked in descending order of `similarity_score`.  ### Attribute Values Shape (`verbose`) By default each nested entity\'s `attribute_values` is a compact object mapping attribute slug to display value, with the ids behind list, reference and file labels in `list_item_ids`, `reference_entity_ids` and `file_ids`. Set `\"verbose\": true` to receive an array of `EntityAttributeValue` items with attribute `id`, `slug`, raw `value`, resolved `custom_value` and `reference_entity_id` instead.
     * @summary Perform semantic vector similarity search on entities
     * @param {SemanticSearchEntitiesRequest} semanticSearchEntitiesRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    semanticSearchEntitiesRaw(requestParameters: SemanticSearchEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SemanticSearchResultItem>>>;

    /**
     * Executes an approximate nearest neighbors (ANN) vector similarity search across entity dimension embeddings.  ### 768-Dimensional Embedding Vectors Requires a 768-dimensional float embedding array (`query_vector`) representing the query text or multimodal vector (e.g. generated by Google `text-embedding-004` or similar models).  ### Scoping & Filtering (`template_id`) Pass an optional `template_id` (UUID) or template slug to constrain the semantic search to records belonging to a specific template schema.  ### Cosine Similarity Threshold & Ranking (`threshold`) - `threshold`: Minimum cosine similarity score threshold (range `0.0` to `1.0`, default `0.5`). Observations below this similarity cutoff are discarded. - Matches are returned strictly ranked in descending order of `similarity_score`.  ### Attribute Values Shape (`verbose`) By default each nested entity\'s `attribute_values` is a compact object mapping attribute slug to display value, with the ids behind list, reference and file labels in `list_item_ids`, `reference_entity_ids` and `file_ids`. Set `\"verbose\": true` to receive an array of `EntityAttributeValue` items with attribute `id`, `slug`, raw `value`, resolved `custom_value` and `reference_entity_id` instead.
     * Perform semantic vector similarity search on entities
     */
    semanticSearchEntities(requestParameters: SemanticSearchEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SemanticSearchResultItem>>;

    /**
     * Creates request options for updateEntity without sending the request
     * @param {string} id Unique entity identifier (UUID)
     * @param {UpdateEntityRequest} updateEntityRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    updateEntityRequestOpts(requestParameters: UpdateEntityOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Partial update: writes the attributes in the map and leaves every other attribute untouched. This is the default way to change an entity. See the `attributes` property for the accepted value shapes.  `attributes` is required and must not be empty. Dimension changes are appended to the entity\'s history (unchanged values cost nothing); metric attributes are appended to the entity\'s time series.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no entity with that id.
     * @summary Update entity attribute values
     * @param {string} id Unique entity identifier (UUID)
     * @param {UpdateEntityRequest} updateEntityRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityApiInterface
     */
    updateEntityRaw(requestParameters: UpdateEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Partial update: writes the attributes in the map and leaves every other attribute untouched. This is the default way to change an entity. See the `attributes` property for the accepted value shapes.  `attributes` is required and must not be empty. Dimension changes are appended to the entity\'s history (unchanged values cost nothing); metric attributes are appended to the entity\'s time series.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no entity with that id.
     * Update entity attribute values
     */
    updateEntity(requestParameters: UpdateEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

}

/**
 * 
 */
export class EntityApi extends runtime.BaseAPI implements EntityApiInterface {

    /**
     * Creates request options for aggregateEntities without sending the request
     */
    async aggregateEntitiesRequestOpts(requestParameters: AggregateEntitiesOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling aggregateEntities().'
            );
        }

        if (requestParameters['aggregateEntitiesRequest'] == null) {
            throw new runtime.RequiredError(
                'aggregateEntitiesRequest',
                'Required parameter "aggregateEntitiesRequest" was null or undefined when calling aggregateEntities().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/aggregate/{template_id}`;
        urlPath = urlPath.replace(`{${"template_id"}}`, encodeURIComponent(String(requestParameters['templateId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AggregateEntitiesRequestToJSON(requestParameters['aggregateEntitiesRequest']),
        };
    }

    /**
     * Answers \"how many\", \"how much\" and \"broken down by\" questions in one call, computed by the database. Use it instead of paginating `search_entities` and tallying rows: a count or a per-status breakdown of a 10,000-record template is one small response. See `filter_groups`, `group_by` and `aggregations` for the request shape.  Each response group\'s `key` mirrors `group_by` in order (`value` is the stored value, `custom_value` is the list item label or the referenced record\'s display value; `null` groups the records that have no value for that attribute), and `aggregates` mirrors `aggregations` in order. Numbers come back as floats, dates as RFC 3339 strings, and `null` when no record in the group has a value. Metric attributes are rejected with a 400: they are time series, reduced over a time window with `get_entity_chart` instead — this endpoint reduces the current dimension values of records.  Groups are ordered by the first aggregation descending (nulls last), then by key. `limit` (1-100, default 50) caps the groups returned; `truncated: true` means more groups exist — narrow with `filter_groups` or group by fewer fields.  Read-only. Applies the caller\'s template access and row scopes exactly as search does; restricted attributes are not valid fields.
     * Count, sum, average, min or max entities, optionally grouped by attributes
     */
    async aggregateEntitiesRaw(requestParameters: AggregateEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AggregateEntities200Response>> {
        const requestOptions = await this.aggregateEntitiesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AggregateEntities200ResponseFromJSON(jsonValue));
    }

    /**
     * Answers \"how many\", \"how much\" and \"broken down by\" questions in one call, computed by the database. Use it instead of paginating `search_entities` and tallying rows: a count or a per-status breakdown of a 10,000-record template is one small response. See `filter_groups`, `group_by` and `aggregations` for the request shape.  Each response group\'s `key` mirrors `group_by` in order (`value` is the stored value, `custom_value` is the list item label or the referenced record\'s display value; `null` groups the records that have no value for that attribute), and `aggregates` mirrors `aggregations` in order. Numbers come back as floats, dates as RFC 3339 strings, and `null` when no record in the group has a value. Metric attributes are rejected with a 400: they are time series, reduced over a time window with `get_entity_chart` instead — this endpoint reduces the current dimension values of records.  Groups are ordered by the first aggregation descending (nulls last), then by key. `limit` (1-100, default 50) caps the groups returned; `truncated: true` means more groups exist — narrow with `filter_groups` or group by fewer fields.  Read-only. Applies the caller\'s template access and row scopes exactly as search does; restricted attributes are not valid fields.
     * Count, sum, average, min or max entities, optionally grouped by attributes
     */
    async aggregateEntities(requestParameters: AggregateEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AggregateEntities200Response> {
        const response = await this.aggregateEntitiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for batchExecuteEntityAction without sending the request
     */
    async batchExecuteEntityActionRequestOpts(requestParameters: BatchExecuteEntityActionOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['slug'] == null) {
            throw new runtime.RequiredError(
                'slug',
                'Required parameter "slug" was null or undefined when calling batchExecuteEntityAction().'
            );
        }

        if (requestParameters['batchExecuteEntityActionRequest'] == null) {
            throw new runtime.RequiredError(
                'batchExecuteEntityActionRequest',
                'Required parameter "batchExecuteEntityActionRequest" was null or undefined when calling batchExecuteEntityAction().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/batch/actions/{slug}`;
        urlPath = urlPath.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters['slug'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BatchExecuteEntityActionRequestToJSON(requestParameters['batchExecuteEntityActionRequest']),
        };
    }

    /**
     * Runs one named action on many records in a single call — \"confirm these forty\". Each record goes through exactly what `POST /entities/{id}/actions/{slug}` (`execute_entity_action`) does: precondition, field matching, presets, type validation and the template\'s rules, with history attributed to the action. `values` are the same for every record.  The action is resolved per record on its template, so the selection may span templates that each define the slug; a record whose template does not is reported as `failed` with a 404 body.  At most 100 records per call; page a larger selection.  Outcomes: By default (`atomic: false`) every record is attempted and the response is `200` with one outcome per record — `executed` with a receipt, or `precondition_failed` / `rule_violated` / `failed` with the error body the single-record endpoint would have returned. Read the counters, then `results` for the records that did not run.  With `atomic: true` the batch runs in one transaction and the first record that does not execute rolls all of it back. That case answers with that record\'s own error status and body plus `failed_entity_id`, not with a results list.  Quotas: The dimension-update quota is checked for the whole selection before any record is written.  Errors: - `400` — the body is not the documented shape, or lists an entity twice. - `402` — the selection would cross the tier\'s update quota. - `422` — a submitted value does not fit the action (only when nothing could run).
     * Execute an action on a selection of entities
     */
    async batchExecuteEntityActionRaw(requestParameters: BatchExecuteEntityActionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchExecuteEntityActionResponse>> {
        const requestOptions = await this.batchExecuteEntityActionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchExecuteEntityActionResponseFromJSON(jsonValue));
    }

    /**
     * Runs one named action on many records in a single call — \"confirm these forty\". Each record goes through exactly what `POST /entities/{id}/actions/{slug}` (`execute_entity_action`) does: precondition, field matching, presets, type validation and the template\'s rules, with history attributed to the action. `values` are the same for every record.  The action is resolved per record on its template, so the selection may span templates that each define the slug; a record whose template does not is reported as `failed` with a 404 body.  At most 100 records per call; page a larger selection.  Outcomes: By default (`atomic: false`) every record is attempted and the response is `200` with one outcome per record — `executed` with a receipt, or `precondition_failed` / `rule_violated` / `failed` with the error body the single-record endpoint would have returned. Read the counters, then `results` for the records that did not run.  With `atomic: true` the batch runs in one transaction and the first record that does not execute rolls all of it back. That case answers with that record\'s own error status and body plus `failed_entity_id`, not with a results list.  Quotas: The dimension-update quota is checked for the whole selection before any record is written.  Errors: - `400` — the body is not the documented shape, or lists an entity twice. - `402` — the selection would cross the tier\'s update quota. - `422` — a submitted value does not fit the action (only when nothing could run).
     * Execute an action on a selection of entities
     */
    async batchExecuteEntityAction(requestParameters: BatchExecuteEntityActionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchExecuteEntityActionResponse> {
        const response = await this.batchExecuteEntityActionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for batchWriteEntities without sending the request
     */
    async batchWriteEntitiesRequestOpts(requestParameters: BatchWriteEntitiesOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['batchWriteEntitiesRequest'] == null) {
            throw new runtime.RequiredError(
                'batchWriteEntitiesRequest',
                'Required parameter "batchWriteEntitiesRequest" was null or undefined when calling batchWriteEntities().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/batch`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BatchWriteEntitiesRequestToJSON(requestParameters['batchWriteEntitiesRequest']),
        };
    }

    /**
     * Applies an ordered, heterogeneous list of entity writes in a single call: update these twenty, create three, replace two, delete one.  This is distinct from CSV import, which moves a homogeneous set of new rows. Use this endpoint when the set of edits is already computed and addresses known records.  Operations: Every entry names an `op` and carries exactly the fields for it — nothing more, nothing less: - `create`  — `{ \"op\": \"create\", \"template\": \"<slug|uuid>\", \"id\"?: \"<uuidv7>\", \"attributes\": { ... } }` (`attributes` may be `{}`) - `update`  — `{ \"op\": \"update\", \"id\": \"<uuid>\", \"attributes\": { ... } }` (non-empty; partial, like PATCH) - `replace` — `{ \"op\": \"replace\", \"id\": \"<uuid>\", \"attributes\": { ... } }` (like PUT: attributes absent from the map are cleared; `{}` clears all; metrics rejected) - `delete`  — `{ \"op\": \"delete\", \"id\": \"<uuid>\" }` (soft delete)  `id` always means the entity id; `template` always means the template slug or UUID. See `BatchOperationInput`\'s `attributes` property for the accepted value shapes.  At most 100 operations per call. Larger sets must be split.  Failure handling: By default (`atomic: false`) every operation is attempted, successes stand, and each failure is reported against its index with the same error body the single-entity endpoint would have returned. The response is `200` regardless of how many entries failed; read `failed` and the per-item `status`.  With `atomic: true` the whole batch runs in one transaction and the first failure rolls all of it back. That case answers with the failing operation\'s own error status and body plus `failed_index`, not with a results list. Atomic batches reject metric attribute values, because metric telemetry is published outside the transaction and cannot be rolled back.  Quotas: Tier quotas are evaluated for the whole batch before any of it is applied, so a batch that would cross the limit is refused as a unit rather than applied halfway.
     * Apply a batch of mixed entity creates, updates, replaces, and deletes
     */
    async batchWriteEntitiesRaw(requestParameters: BatchWriteEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BatchWriteEntitiesResponse>> {
        const requestOptions = await this.batchWriteEntitiesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BatchWriteEntitiesResponseFromJSON(jsonValue));
    }

    /**
     * Applies an ordered, heterogeneous list of entity writes in a single call: update these twenty, create three, replace two, delete one.  This is distinct from CSV import, which moves a homogeneous set of new rows. Use this endpoint when the set of edits is already computed and addresses known records.  Operations: Every entry names an `op` and carries exactly the fields for it — nothing more, nothing less: - `create`  — `{ \"op\": \"create\", \"template\": \"<slug|uuid>\", \"id\"?: \"<uuidv7>\", \"attributes\": { ... } }` (`attributes` may be `{}`) - `update`  — `{ \"op\": \"update\", \"id\": \"<uuid>\", \"attributes\": { ... } }` (non-empty; partial, like PATCH) - `replace` — `{ \"op\": \"replace\", \"id\": \"<uuid>\", \"attributes\": { ... } }` (like PUT: attributes absent from the map are cleared; `{}` clears all; metrics rejected) - `delete`  — `{ \"op\": \"delete\", \"id\": \"<uuid>\" }` (soft delete)  `id` always means the entity id; `template` always means the template slug or UUID. See `BatchOperationInput`\'s `attributes` property for the accepted value shapes.  At most 100 operations per call. Larger sets must be split.  Failure handling: By default (`atomic: false`) every operation is attempted, successes stand, and each failure is reported against its index with the same error body the single-entity endpoint would have returned. The response is `200` regardless of how many entries failed; read `failed` and the per-item `status`.  With `atomic: true` the whole batch runs in one transaction and the first failure rolls all of it back. That case answers with the failing operation\'s own error status and body plus `failed_index`, not with a results list. Atomic batches reject metric attribute values, because metric telemetry is published outside the transaction and cannot be rolled back.  Quotas: Tier quotas are evaluated for the whole batch before any of it is applied, so a batch that would cross the limit is refused as a unit rather than applied halfway.
     * Apply a batch of mixed entity creates, updates, replaces, and deletes
     */
    async batchWriteEntities(requestParameters: BatchWriteEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BatchWriteEntitiesResponse> {
        const response = await this.batchWriteEntitiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createEntity without sending the request
     */
    async createEntityRequestOpts(requestParameters: CreateEntityOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['template'] == null) {
            throw new runtime.RequiredError(
                'template',
                'Required parameter "template" was null or undefined when calling createEntity().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/template/{template}`;
        urlPath = urlPath.replace(`{${"template"}}`, encodeURIComponent(String(requestParameters['template'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateEntityRequestToJSON(requestParameters['createEntityRequest']),
        };
    }

    /**
     * Creates one entity of the template named in the path — `{template}` is the template\'s slug or UUID — and returns its id. Pass `id` to choose the entity\'s UUIDv7 yourself (cross-system keys); otherwise one is generated. An `id` that already exists — even a soft-deleted entity\'s — is rejected with `409` rather than overwritten: if a retry might be hitting this because an earlier call\'s response was lost, `GET /entities/{id}` first to check whether it already carries what you meant to write, rather than retrying blindly.  See the `attributes` property for the accepted value shapes.  `attributes` is required; send `{}` to create an entity with no values yet. Metric attributes in the map are appended to the entity\'s time series; everything else becomes the entity\'s initial state and is recorded in its history.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no template with that slug or UUID in this project. - `409` — the given `id` already exists.
     * Create a new dynamic entity
     */
    async createEntityRaw(requestParameters: CreateEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreateEntity201Response>> {
        const requestOptions = await this.createEntityRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateEntity201ResponseFromJSON(jsonValue));
    }

    /**
     * Creates one entity of the template named in the path — `{template}` is the template\'s slug or UUID — and returns its id. Pass `id` to choose the entity\'s UUIDv7 yourself (cross-system keys); otherwise one is generated. An `id` that already exists — even a soft-deleted entity\'s — is rejected with `409` rather than overwritten: if a retry might be hitting this because an earlier call\'s response was lost, `GET /entities/{id}` first to check whether it already carries what you meant to write, rather than retrying blindly.  See the `attributes` property for the accepted value shapes.  `attributes` is required; send `{}` to create an entity with no values yet. Metric attributes in the map are appended to the entity\'s time series; everything else becomes the entity\'s initial state and is recorded in its history.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no template with that slug or UUID in this project. - `409` — the given `id` already exists.
     * Create a new dynamic entity
     */
    async createEntity(requestParameters: CreateEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreateEntity201Response> {
        const response = await this.createEntityRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteEntity without sending the request
     */
    async deleteEntityRequestOpts(requestParameters: DeleteEntityRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteEntity().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Marks an entity record as soft-deleted by setting its `deleted_at` timestamp.  Soft-deleted entities are immediately excluded from standard entity searches, BI row queries, and direct retrieval endpoints. Associated historical change logs and time-series telemetry remain preserved for audit compliance.
     * Soft-delete an entity record
     */
    async deleteEntityRaw(requestParameters: DeleteEntityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deleteEntityRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Marks an entity record as soft-deleted by setting its `deleted_at` timestamp.  Soft-deleted entities are immediately excluded from standard entity searches, BI row queries, and direct retrieval endpoints. Associated historical change logs and time-series telemetry remain preserved for audit compliance.
     * Soft-delete an entity record
     */
    async deleteEntity(requestParameters: DeleteEntityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteEntityRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for executeEntityAction without sending the request
     */
    async executeEntityActionRequestOpts(requestParameters: ExecuteEntityActionOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling executeEntityAction().'
            );
        }

        if (requestParameters['slug'] == null) {
            throw new runtime.RequiredError(
                'slug',
                'Required parameter "slug" was null or undefined when calling executeEntityAction().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/{id}/actions/{slug}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));
        urlPath = urlPath.replace(`{${"slug"}}`, encodeURIComponent(String(requestParameters['slug'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ExecuteEntityActionRequestToJSON(requestParameters['executeEntityActionRequest']),
        };
    }

    /**
     * Runs one named action on one record as a single atomic write. Prefer this over a plain update (`PATCH /entities/{id}`, the `update_entity` tool) whenever `GET /entities/{id}/actions` (`list_entity_actions`) lists an action for what you intend: the action\'s presets are applied for you and its required fields are enforced.  What happens: 1. The action\'s precondition is checked against the record\'s current values — `409` if it does not hold, with the reason. 2. `values` are matched to the action\'s `fields`; an attribute the action does not ask for, or an empty required field, is `422` keyed by `attributes.<slug>`. 3. Presets are merged on top of `values` (presets win) and the result is written exactly like an entity update: attribute types are validated and the template\'s rules are enforced (`422` on a violation, in the same `attributes.<slug>` shape).  The response is a receipt naming what was written — for a number field sent as `{ \"op\": \"increment\", \"value\": n }` that is the operation itself; the resolved number lands in the record and its history. History rows produced by the write carry the action\'s slug.  Errors: - `400` — the body is not the documented shape. - `403` — the caller may not edit this record. - `404` — no such entity, or the template has no enabled action with this slug. - `409` — the precondition does not hold; `detail` says which attribute and why. - `422` — a required field is empty, a value fails its attribute type, or a rule refuses the write.
     * Execute an action on an entity
     */
    async executeEntityActionRaw(requestParameters: ExecuteEntityActionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ExecuteEntityActionResponse>> {
        const requestOptions = await this.executeEntityActionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ExecuteEntityActionResponseFromJSON(jsonValue));
    }

    /**
     * Runs one named action on one record as a single atomic write. Prefer this over a plain update (`PATCH /entities/{id}`, the `update_entity` tool) whenever `GET /entities/{id}/actions` (`list_entity_actions`) lists an action for what you intend: the action\'s presets are applied for you and its required fields are enforced.  What happens: 1. The action\'s precondition is checked against the record\'s current values — `409` if it does not hold, with the reason. 2. `values` are matched to the action\'s `fields`; an attribute the action does not ask for, or an empty required field, is `422` keyed by `attributes.<slug>`. 3. Presets are merged on top of `values` (presets win) and the result is written exactly like an entity update: attribute types are validated and the template\'s rules are enforced (`422` on a violation, in the same `attributes.<slug>` shape).  The response is a receipt naming what was written — for a number field sent as `{ \"op\": \"increment\", \"value\": n }` that is the operation itself; the resolved number lands in the record and its history. History rows produced by the write carry the action\'s slug.  Errors: - `400` — the body is not the documented shape. - `403` — the caller may not edit this record. - `404` — no such entity, or the template has no enabled action with this slug. - `409` — the precondition does not hold; `detail` says which attribute and why. - `422` — a required field is empty, a value fails its attribute type, or a rule refuses the write.
     * Execute an action on an entity
     */
    async executeEntityAction(requestParameters: ExecuteEntityActionOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ExecuteEntityActionResponse> {
        const response = await this.executeEntityActionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for exportEntities without sending the request
     */
    async exportEntitiesRequestOpts(requestParameters: ExportEntitiesOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling exportEntities().'
            );
        }

        if (requestParameters['exportEntitiesRequest'] == null) {
            throw new runtime.RequiredError(
                'exportEntitiesRequest',
                'Required parameter "exportEntitiesRequest" was null or undefined when calling exportEntities().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['sortField'] != null) {
            queryParameters['sort_field'] = requestParameters['sortField'];
        }

        if (requestParameters['sortDirection'] != null) {
            queryParameters['sort_direction'] = requestParameters['sortDirection'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/export/{template_id}`;
        urlPath = urlPath.replace(`{${"template_id"}}`, encodeURIComponent(String(requestParameters['templateId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ExportEntitiesRequestToJSON(requestParameters['exportEntitiesRequest']),
        };
    }

    /**
     * Exports entity records of a template schema matching filter criteria as a streaming CSV download.  ### Re-importable CSV Schema Format The generated CSV conforms to the Omnismith two-row metadata specification, making it directly compatible with `POST /entities/import/{template_id}`: - **Row 1**: Display column names and attribute aliases. - **Row 2**: Metadata row prefixed with `#` containing attribute UUIDs and column IDs (e.g. `#id`, `#018b2f1b-8c1a...`). - **Row 3+**: Entity data records.  Accepts the same `filter_groups` and `global_search` payload as `searchEntities`.  ### Filters (`filter_groups`) A list of groups; clauses inside a group are AND-ed, groups are OR-ed. `[[a, b]]` is `a AND b`; `[[a], [b, c]]` is `a OR (b AND c)`; `[]` applies no filter. ```json [   [     {\"field\": \"status\", \"operator\": \"in\", \"value\": [\"018b…0020\", \"018b…0021\"]},     {\"field\": \"created_at\", \"operator\": \"between\", \"value\": [\"2026-01-01\", \"2026-03-31\"]},     {\"field\": \"customer.tier\", \"operator\": \"eq\", \"value\": \"018b…0042\"}   ],   [{\"field\": \"priority\", \"operator\": \"eq\", \"value\": \"018b…0007\"}] ] ``` - **`field`**: attribute slug or UUID, a standard field (`id`, `created_at`, `updated_at`), or a one-hop path `<reference>.<attribute>` that filters on an attribute of the referenced record (e.g. `customer.tier`). One hop only. - **`operator`** and **`value`**: `eq`, `neq`, `gt`, `lt`, `like` (case-insensitive substring), `not-like` take a string; `in`, `not-in` take a non-empty list of strings; `between` takes `[lower, upper]` (inclusive; number, date, datetime attributes and `created_at` / `updated_at`); `empty`, `not-empty` take no value. - List and reference attributes compare the stored id (from `list_item_ids` / `reference_entity_ids` or the schema), never the label. - Unknown fields, operators that do not fit the field, malformed values and paths that do not traverse a reference are refused with 400 naming the valid fields; a path into a template the caller may not view is 403.  ### Sorting Sort results via `sort_field` (attribute UUID, slug, or standard timestamp) and `sort_direction` (`asc`/`desc`).
     * Export entities to structured CSV file
     */
    async exportEntitiesRaw(requestParameters: ExportEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Blob>> {
        const requestOptions = await this.exportEntitiesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.BlobApiResponse(response);
    }

    /**
     * Exports entity records of a template schema matching filter criteria as a streaming CSV download.  ### Re-importable CSV Schema Format The generated CSV conforms to the Omnismith two-row metadata specification, making it directly compatible with `POST /entities/import/{template_id}`: - **Row 1**: Display column names and attribute aliases. - **Row 2**: Metadata row prefixed with `#` containing attribute UUIDs and column IDs (e.g. `#id`, `#018b2f1b-8c1a...`). - **Row 3+**: Entity data records.  Accepts the same `filter_groups` and `global_search` payload as `searchEntities`.  ### Filters (`filter_groups`) A list of groups; clauses inside a group are AND-ed, groups are OR-ed. `[[a, b]]` is `a AND b`; `[[a], [b, c]]` is `a OR (b AND c)`; `[]` applies no filter. ```json [   [     {\"field\": \"status\", \"operator\": \"in\", \"value\": [\"018b…0020\", \"018b…0021\"]},     {\"field\": \"created_at\", \"operator\": \"between\", \"value\": [\"2026-01-01\", \"2026-03-31\"]},     {\"field\": \"customer.tier\", \"operator\": \"eq\", \"value\": \"018b…0042\"}   ],   [{\"field\": \"priority\", \"operator\": \"eq\", \"value\": \"018b…0007\"}] ] ``` - **`field`**: attribute slug or UUID, a standard field (`id`, `created_at`, `updated_at`), or a one-hop path `<reference>.<attribute>` that filters on an attribute of the referenced record (e.g. `customer.tier`). One hop only. - **`operator`** and **`value`**: `eq`, `neq`, `gt`, `lt`, `like` (case-insensitive substring), `not-like` take a string; `in`, `not-in` take a non-empty list of strings; `between` takes `[lower, upper]` (inclusive; number, date, datetime attributes and `created_at` / `updated_at`); `empty`, `not-empty` take no value. - List and reference attributes compare the stored id (from `list_item_ids` / `reference_entity_ids` or the schema), never the label. - Unknown fields, operators that do not fit the field, malformed values and paths that do not traverse a reference are refused with 400 naming the valid fields; a path into a template the caller may not view is 403.  ### Sorting Sort results via `sort_field` (attribute UUID, slug, or standard timestamp) and `sort_direction` (`asc`/`desc`).
     * Export entities to structured CSV file
     */
    async exportEntities(requestParameters: ExportEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Blob> {
        const response = await this.exportEntitiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getEntity without sending the request
     */
    async getEntityRequestOpts(requestParameters: GetEntityRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getEntity().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['verbose'] != null) {
            queryParameters['verbose'] = requestParameters['verbose'];
        }

        if (requestParameters['fields'] != null) {
            queryParameters['fields'] = requestParameters['fields']!.join(runtime.COLLECTION_FORMATS["csv"]);
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Retrieves the full hydrated record for a dynamic entity by its unique identifier (UUID). See `verbose` and `fields` for response shape and projection options.
     * Get an entity record by ID
     */
    async getEntityRaw(requestParameters: GetEntityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityResponse>> {
        const requestOptions = await this.getEntityRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntityResponseFromJSON(jsonValue));
    }

    /**
     * Retrieves the full hydrated record for a dynamic entity by its unique identifier (UUID). See `verbose` and `fields` for response shape and projection options.
     * Get an entity record by ID
     */
    async getEntity(requestParameters: GetEntityRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityResponse> {
        const response = await this.getEntityRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getEntityChart without sending the request
     */
    async getEntityChartRequestOpts(requestParameters: GetEntityChartRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getEntityChart().'
            );
        }

        if (requestParameters['attributeIds'] == null) {
            throw new runtime.RequiredError(
                'attributeIds',
                'Required parameter "attributeIds" was null or undefined when calling getEntityChart().'
            );
        }

        if (requestParameters['start'] == null) {
            throw new runtime.RequiredError(
                'start',
                'Required parameter "start" was null or undefined when calling getEntityChart().'
            );
        }

        if (requestParameters['end'] == null) {
            throw new runtime.RequiredError(
                'end',
                'Required parameter "end" was null or undefined when calling getEntityChart().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['attributeIds'] != null) {
            queryParameters['attribute_ids'] = requestParameters['attributeIds'];
        }

        if (requestParameters['aggregateFunc'] != null) {
            queryParameters['aggregate_func'] = requestParameters['aggregateFunc'];
        }

        if (requestParameters['bucketWidth'] != null) {
            queryParameters['bucket_width'] = requestParameters['bucketWidth'];
        }

        if (requestParameters['start'] != null) {
            queryParameters['start'] = requestParameters['start'];
        }

        if (requestParameters['end'] != null) {
            queryParameters['end'] = requestParameters['end'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/{id}/chart`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Retrieves aggregated, time-bucketed metric time-series data for an entity. Returns one `series` entry per requested `attribute_id`, each a chronological list of `{time, value}` points aggregated at the given `bucket_width`.
     * Get entity chart time-series data
     */
    async getEntityChartRaw(requestParameters: GetEntityChartRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetEntityChart200Response>> {
        const requestOptions = await this.getEntityChartRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetEntityChart200ResponseFromJSON(jsonValue));
    }

    /**
     * Retrieves aggregated, time-bucketed metric time-series data for an entity. Returns one `series` entry per requested `attribute_id`, each a chronological list of `{time, value}` points aggregated at the given `bucket_width`.
     * Get entity chart time-series data
     */
    async getEntityChart(requestParameters: GetEntityChartRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetEntityChart200Response> {
        const response = await this.getEntityChartRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getEntityHistory without sending the request
     */
    async getEntityHistoryRequestOpts(requestParameters: GetEntityHistoryRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getEntityHistory().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['sortBy'] != null) {
            queryParameters['sort_by'] = requestParameters['sortBy'];
        }

        if (requestParameters['sortDirection'] != null) {
            queryParameters['sort_direction'] = requestParameters['sortDirection'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['attributeIds'] != null) {
            queryParameters['attribute_ids'] = requestParameters['attributeIds'];
        }

        if (requestParameters['start'] != null) {
            queryParameters['start'] = (requestParameters['start'] as any).toISOString();
        }

        if (requestParameters['end'] != null) {
            queryParameters['end'] = (requestParameters['end'] as any).toISOString();
        }

        if (requestParameters['authorEmail'] != null) {
            queryParameters['author_email'] = requestParameters['authorEmail'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/{id}/history`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Retrieves the immutable change audit log for an entity\'s dimension attribute mutations. Records mutations to dimension, list, and reference attribute values; metric telemetry is excluded and lives in time-series storage instead (see `get_entity_chart`). Returns paginated `items` (each an attribute change from `old_value` to `value`, with `author_email` and `action_slug` when the write came from an action) plus `total` matching records.
     * Get entity dimension change history
     */
    async getEntityHistoryRaw(requestParameters: GetEntityHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetEntityHistory200Response>> {
        const requestOptions = await this.getEntityHistoryRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetEntityHistory200ResponseFromJSON(jsonValue));
    }

    /**
     * Retrieves the immutable change audit log for an entity\'s dimension attribute mutations. Records mutations to dimension, list, and reference attribute values; metric telemetry is excluded and lives in time-series storage instead (see `get_entity_chart`). Returns paginated `items` (each an attribute change from `old_value` to `value`, with `author_email` and `action_slug` when the write came from an action) plus `total` matching records.
     * Get entity dimension change history
     */
    async getEntityHistory(requestParameters: GetEntityHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetEntityHistory200Response> {
        const response = await this.getEntityHistoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for importEntities without sending the request
     */
    async importEntitiesRequestOpts(requestParameters: ImportEntitiesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling importEntities().'
            );
        }

        if (requestParameters['file'] == null) {
            throw new runtime.RequiredError(
                'file',
                'Required parameter "file" was null or undefined when calling importEntities().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['file'] != null) {
            formParams.append('file', requestParameters['file'] as any);
        }


        let urlPath = `/entities/import/{template_id}`;
        urlPath = urlPath.replace(`{${"template_id"}}`, encodeURIComponent(String(requestParameters['templateId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Bulk imports entity records into a template schema from a structured CSV file.  ### Upsert Semantics - **Update existing**: If a data row includes an `id` matching an existing entity UUID, that entity is updated. - **Create new**: If the `id` column is empty, omitted, or contains a new UUID, a new entity record is created.  ### Required 2-Row CSV Header Format The CSV file must follow the Omnismith two-row header format (identical to the output of `POST /entities/export/{template_id}`): - **Row 1 (Display Header)**: Human-readable attribute names or aliases (e.g. `ID`, `SKU`, `Price`, `Category`). - **Row 2 (Metadata Marker)**: Canonical attribute identifiers prefixed by `#` (e.g. `#id`, `#018b2f1b-8c1a...`, `#018b2f1b-8c1b...`). - **Row 3+ (Data Rows)**: Serialized entity values conforming to the template\'s attribute data types.  ### Attribute Value Validation - List attributes require valid `ListItem` option UUIDs. - Reference attributes require existing target `Entity` UUIDs. - Number/Date/Boolean fields must match required format syntax.  ### Execution Summary Returns an execution report detailing counts of created, updated, skipped, and failed rows, along with granular row/column error messages.
     * Import entities from structured CSV file
     */
    async importEntitiesRaw(requestParameters: ImportEntitiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ImportEntities200Response>> {
        const requestOptions = await this.importEntitiesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ImportEntities200ResponseFromJSON(jsonValue));
    }

    /**
     * Bulk imports entity records into a template schema from a structured CSV file.  ### Upsert Semantics - **Update existing**: If a data row includes an `id` matching an existing entity UUID, that entity is updated. - **Create new**: If the `id` column is empty, omitted, or contains a new UUID, a new entity record is created.  ### Required 2-Row CSV Header Format The CSV file must follow the Omnismith two-row header format (identical to the output of `POST /entities/export/{template_id}`): - **Row 1 (Display Header)**: Human-readable attribute names or aliases (e.g. `ID`, `SKU`, `Price`, `Category`). - **Row 2 (Metadata Marker)**: Canonical attribute identifiers prefixed by `#` (e.g. `#id`, `#018b2f1b-8c1a...`, `#018b2f1b-8c1b...`). - **Row 3+ (Data Rows)**: Serialized entity values conforming to the template\'s attribute data types.  ### Attribute Value Validation - List attributes require valid `ListItem` option UUIDs. - Reference attributes require existing target `Entity` UUIDs. - Number/Date/Boolean fields must match required format syntax.  ### Execution Summary Returns an execution report detailing counts of created, updated, skipped, and failed rows, along with granular row/column error messages.
     * Import entities from structured CSV file
     */
    async importEntities(requestParameters: ImportEntitiesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ImportEntities200Response> {
        const response = await this.importEntitiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for ingestEntityMetrics without sending the request
     */
    async ingestEntityMetricsRequestOpts(requestParameters: IngestEntityMetricsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling ingestEntityMetrics().'
            );
        }

        if (requestParameters['ingestMetricsRequest'] == null) {
            throw new runtime.RequiredError(
                'ingestMetricsRequest',
                'Required parameter "ingestMetricsRequest" was null or undefined when calling ingestEntityMetrics().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/{id}/metrics`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: IngestMetricsRequestToJSON(requestParameters['ingestMetricsRequest']),
        };
    }

    /**
     * Ingests time-series metric observations for an entity record.  Batch Telemetry Ingestion: Accepts a batch array of metric observations (`metric_values`). Each observation targets a metric attribute by `attribute_id` (UUID) or `attribute_slug` and specifies a numeric `value`.  Ingestion is asynchronous: a call is accepted immediately (`202`) and each observation is persisted shortly after, so it may not be visible in `get_entity_chart` the instant this call returns.  Strict Metric Attribute Constraint: Only attributes defined with `attribute_type: Metric` are accepted by this endpoint. Mutations to dimension, list, or reference attributes must use `PATCH /entities/{id}` instead.
     * Ingest high-frequency metric observations for an entity
     */
    async ingestEntityMetricsRaw(requestParameters: IngestEntityMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.ingestEntityMetricsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Ingests time-series metric observations for an entity record.  Batch Telemetry Ingestion: Accepts a batch array of metric observations (`metric_values`). Each observation targets a metric attribute by `attribute_id` (UUID) or `attribute_slug` and specifies a numeric `value`.  Ingestion is asynchronous: a call is accepted immediately (`202`) and each observation is persisted shortly after, so it may not be visible in `get_entity_chart` the instant this call returns.  Strict Metric Attribute Constraint: Only attributes defined with `attribute_type: Metric` are accepted by this endpoint. Mutations to dimension, list, or reference attributes must use `PATCH /entities/{id}` instead.
     * Ingest high-frequency metric observations for an entity
     */
    async ingestEntityMetrics(requestParameters: IngestEntityMetricsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.ingestEntityMetricsRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for listEntityActions without sending the request
     */
    async listEntityActionsRequestOpts(requestParameters: ListEntityActionsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling listEntityActions().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/{id}/actions`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * The enabled actions of the entity\'s template, each evaluated against the record\'s current values.  Call this before changing a record: when an action exists for what you intend (a status transition, a hand-off), run it with `POST /entities/{id}/actions/{slug}` (the `execute_entity_action` tool) instead of a plain update (`update_entity`), so its presets and required fields apply.  Each entry says whether the action is `available` now and, if not, `unavailable_reason` names the attribute, the expectation and the current value. `fields` are the values to submit (keyed by `slug` in the execute body), with the list choices and reference target resolved; `presets` are what the action will set on its own.  Disabled actions are not listed. Read-only: nothing is written.
     * List the actions available on an entity
     */
    async listEntityActionsRaw(requestParameters: ListEntityActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListEntityActions200Response>> {
        const requestOptions = await this.listEntityActionsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListEntityActions200ResponseFromJSON(jsonValue));
    }

    /**
     * The enabled actions of the entity\'s template, each evaluated against the record\'s current values.  Call this before changing a record: when an action exists for what you intend (a status transition, a hand-off), run it with `POST /entities/{id}/actions/{slug}` (the `execute_entity_action` tool) instead of a plain update (`update_entity`), so its presets and required fields apply.  Each entry says whether the action is `available` now and, if not, `unavailable_reason` names the attribute, the expectation and the current value. `fields` are the values to submit (keyed by `slug` in the execute body), with the list choices and reference target resolved; `presets` are what the action will set on its own.  Disabled actions are not listed. Read-only: nothing is written.
     * List the actions available on an entity
     */
    async listEntityActions(requestParameters: ListEntityActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListEntityActions200Response> {
        const response = await this.listEntityActionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for replaceEntity without sending the request
     */
    async replaceEntityRequestOpts(requestParameters: ReplaceEntityOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling replaceEntity().'
            );
        }

        if (requestParameters['replaceEntityRequest'] == null) {
            throw new runtime.RequiredError(
                'replaceEntityRequest',
                'Required parameter "replaceEntityRequest" was null or undefined when calling replaceEntity().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ReplaceEntityRequestToJSON(requestParameters['replaceEntityRequest']),
        };
    }

    /**
     * Full replacement: after this call the entity\'s dimension attributes are exactly the map you sent. Every non-metric attribute of the template that is **absent from the map is cleared**. Use PATCH unless you really mean \"make the record look exactly like this\".  ### `attributes` An object keyed by attribute **slug or UUID** — mix them freely. Each value is a plain scalar, a backfill object `{ \"value\": ..., \"updated_at\": \"<RFC 3339>\" }`, or an operation object `{ \"op\": \"increment\", \"value\": <number> }`:  ```json {   \"hostname\": \"edge-fra-01\",   \"cpu_cores\": 8,   \"is_active\": true,   \"notes\": null,   \"01a094f1-24be-7154-a5bd-3b5c33c930fb\": \"01a094f1-4c1d-7498-b73b-48ae46da900b\",   \"operational_status\": { \"value\": \"Active\", \"updated_at\": \"2026-09-12T12:23:52Z\" },   \"restart_count\": { \"op\": \"increment\", \"value\": 1 } } ```  `null` clears an attribute. `{ \"op\": \"increment\", \"value\": n }` adds `n` to the stored number without you reading it first (number attributes and metrics only; concurrent increments never lose an update). Every attribute must belong to the entity\'s template and may appear only once (the same attribute as slug *and* UUID is rejected). Full value rules are on the `EntityAttributesInput` schema.  `attributes` is required; an explicit `{}` clears every non-metric attribute. Metric attributes are append-only telemetry and cannot be replaced — including one in the map is a `422`; send it via PATCH or `/entities/{id}/metrics`.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no entity with that id.
     * Replace all non-metric attributes of an entity
     */
    async replaceEntityRaw(requestParameters: ReplaceEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.replaceEntityRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Full replacement: after this call the entity\'s dimension attributes are exactly the map you sent. Every non-metric attribute of the template that is **absent from the map is cleared**. Use PATCH unless you really mean \"make the record look exactly like this\".  ### `attributes` An object keyed by attribute **slug or UUID** — mix them freely. Each value is a plain scalar, a backfill object `{ \"value\": ..., \"updated_at\": \"<RFC 3339>\" }`, or an operation object `{ \"op\": \"increment\", \"value\": <number> }`:  ```json {   \"hostname\": \"edge-fra-01\",   \"cpu_cores\": 8,   \"is_active\": true,   \"notes\": null,   \"01a094f1-24be-7154-a5bd-3b5c33c930fb\": \"01a094f1-4c1d-7498-b73b-48ae46da900b\",   \"operational_status\": { \"value\": \"Active\", \"updated_at\": \"2026-09-12T12:23:52Z\" },   \"restart_count\": { \"op\": \"increment\", \"value\": 1 } } ```  `null` clears an attribute. `{ \"op\": \"increment\", \"value\": n }` adds `n` to the stored number without you reading it first (number attributes and metrics only; concurrent increments never lose an update). Every attribute must belong to the entity\'s template and may appear only once (the same attribute as slug *and* UUID is rejected). Full value rules are on the `EntityAttributesInput` schema.  `attributes` is required; an explicit `{}` clears every non-metric attribute. Metric attributes are append-only telemetry and cannot be replaced — including one in the map is a `422`; send it via PATCH or `/entities/{id}/metrics`.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no entity with that id.
     * Replace all non-metric attributes of an entity
     */
    async replaceEntity(requestParameters: ReplaceEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.replaceEntityRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for searchEntities without sending the request
     */
    async searchEntitiesRequestOpts(requestParameters: SearchEntitiesOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling searchEntities().'
            );
        }

        if (requestParameters['searchEntitiesRequest'] == null) {
            throw new runtime.RequiredError(
                'searchEntitiesRequest',
                'Required parameter "searchEntitiesRequest" was null or undefined when calling searchEntities().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        if (requestParameters['sortField'] != null) {
            queryParameters['sort_field'] = requestParameters['sortField'];
        }

        if (requestParameters['sortDirection'] != null) {
            queryParameters['sort_direction'] = requestParameters['sortDirection'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/search/{template_id}`;
        urlPath = urlPath.replace(`{${"template_id"}}`, encodeURIComponent(String(requestParameters['templateId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SearchEntitiesRequestToJSON(requestParameters['searchEntitiesRequest']),
        };
    }

    /**
     * Executes structured queries, full-text search, and sorting across dynamic entities of a template (`template_id` accepts a UUID or a human-readable slug). See `filter_groups`, `global_search`, `verbose`, `fields`, `sort_field` and `sort_direction` for the query and response-shape options. Use `aggregate_entities` instead of paginating through results when you only need counts, sums, or other per-group reduces.
     * Search entities with filtering, sorting, and pagination
     */
    async searchEntitiesRaw(requestParameters: SearchEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SearchEntities200Response>> {
        const requestOptions = await this.searchEntitiesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SearchEntities200ResponseFromJSON(jsonValue));
    }

    /**
     * Executes structured queries, full-text search, and sorting across dynamic entities of a template (`template_id` accepts a UUID or a human-readable slug). See `filter_groups`, `global_search`, `verbose`, `fields`, `sort_field` and `sort_direction` for the query and response-shape options. Use `aggregate_entities` instead of paginating through results when you only need counts, sums, or other per-group reduces.
     * Search entities with filtering, sorting, and pagination
     */
    async searchEntities(requestParameters: SearchEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SearchEntities200Response> {
        const response = await this.searchEntitiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for semanticSearchEntities without sending the request
     */
    async semanticSearchEntitiesRequestOpts(requestParameters: SemanticSearchEntitiesOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['semanticSearchEntitiesRequest'] == null) {
            throw new runtime.RequiredError(
                'semanticSearchEntitiesRequest',
                'Required parameter "semanticSearchEntitiesRequest" was null or undefined when calling semanticSearchEntities().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/semantic-search`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SemanticSearchEntitiesRequestToJSON(requestParameters['semanticSearchEntitiesRequest']),
        };
    }

    /**
     * Executes an approximate nearest neighbors (ANN) vector similarity search across entity dimension embeddings.  ### 768-Dimensional Embedding Vectors Requires a 768-dimensional float embedding array (`query_vector`) representing the query text or multimodal vector (e.g. generated by Google `text-embedding-004` or similar models).  ### Scoping & Filtering (`template_id`) Pass an optional `template_id` (UUID) or template slug to constrain the semantic search to records belonging to a specific template schema.  ### Cosine Similarity Threshold & Ranking (`threshold`) - `threshold`: Minimum cosine similarity score threshold (range `0.0` to `1.0`, default `0.5`). Observations below this similarity cutoff are discarded. - Matches are returned strictly ranked in descending order of `similarity_score`.  ### Attribute Values Shape (`verbose`) By default each nested entity\'s `attribute_values` is a compact object mapping attribute slug to display value, with the ids behind list, reference and file labels in `list_item_ids`, `reference_entity_ids` and `file_ids`. Set `\"verbose\": true` to receive an array of `EntityAttributeValue` items with attribute `id`, `slug`, raw `value`, resolved `custom_value` and `reference_entity_id` instead.
     * Perform semantic vector similarity search on entities
     */
    async semanticSearchEntitiesRaw(requestParameters: SemanticSearchEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SemanticSearchResultItem>>> {
        const requestOptions = await this.semanticSearchEntitiesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SemanticSearchResultItemFromJSON));
    }

    /**
     * Executes an approximate nearest neighbors (ANN) vector similarity search across entity dimension embeddings.  ### 768-Dimensional Embedding Vectors Requires a 768-dimensional float embedding array (`query_vector`) representing the query text or multimodal vector (e.g. generated by Google `text-embedding-004` or similar models).  ### Scoping & Filtering (`template_id`) Pass an optional `template_id` (UUID) or template slug to constrain the semantic search to records belonging to a specific template schema.  ### Cosine Similarity Threshold & Ranking (`threshold`) - `threshold`: Minimum cosine similarity score threshold (range `0.0` to `1.0`, default `0.5`). Observations below this similarity cutoff are discarded. - Matches are returned strictly ranked in descending order of `similarity_score`.  ### Attribute Values Shape (`verbose`) By default each nested entity\'s `attribute_values` is a compact object mapping attribute slug to display value, with the ids behind list, reference and file labels in `list_item_ids`, `reference_entity_ids` and `file_ids`. Set `\"verbose\": true` to receive an array of `EntityAttributeValue` items with attribute `id`, `slug`, raw `value`, resolved `custom_value` and `reference_entity_id` instead.
     * Perform semantic vector similarity search on entities
     */
    async semanticSearchEntities(requestParameters: SemanticSearchEntitiesOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SemanticSearchResultItem>> {
        const response = await this.semanticSearchEntitiesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateEntity without sending the request
     */
    async updateEntityRequestOpts(requestParameters: UpdateEntityOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateEntity().'
            );
        }

        if (requestParameters['updateEntityRequest'] == null) {
            throw new runtime.RequiredError(
                'updateEntityRequest',
                'Required parameter "updateEntityRequest" was null or undefined when calling updateEntity().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/entities/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateEntityRequestToJSON(requestParameters['updateEntityRequest']),
        };
    }

    /**
     * Partial update: writes the attributes in the map and leaves every other attribute untouched. This is the default way to change an entity. See the `attributes` property for the accepted value shapes.  `attributes` is required and must not be empty. Dimension changes are appended to the entity\'s history (unchanged values cost nothing); metric attributes are appended to the entity\'s time series.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no entity with that id.
     * Update entity attribute values
     */
    async updateEntityRaw(requestParameters: UpdateEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.updateEntityRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Partial update: writes the attributes in the map and leaves every other attribute untouched. This is the default way to change an entity. See the `attributes` property for the accepted value shapes.  `attributes` is required and must not be empty. Dimension changes are appended to the entity\'s history (unchanged values cost nothing); metric attributes are appended to the entity\'s time series.  Errors: - `400` — the body is not the documented shape (missing `attributes`, a list instead of an object, unknown fields, wrong types). - `422` — the shape is right but the content is not: unknown attribute, attribute not on the template, duplicate attribute, bad `updated_at`, a value that fails its attribute type, or an operation on an attribute that is not a number. `errors` names the exact field, e.g. `attributes.status`. - `404` — no entity with that id.
     * Update entity attribute values
     */
    async updateEntity(requestParameters: UpdateEntityOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.updateEntityRaw(requestParameters, initOverrides);
    }

}

/**
  * @export
  * @enum {string}
  */
export enum ExportEntitiesOperationSortDirectionEnum {
    asc = 'asc',
    desc = 'desc'
}
/**
  * @export
  * @enum {string}
  */
export enum GetEntityChartAggregateFuncEnum {
    sum = 'sum',
    avg = 'avg',
    min = 'min',
    max = 'max',
    count = 'count',
    first = 'first',
    last = 'last'
}
/**
  * @export
  * @enum {string}
  */
export enum GetEntityChartBucketWidthEnum {
    _1_second = '1 second',
    _5_seconds = '5 seconds',
    _10_seconds = '10 seconds',
    _1_minute = '1 minute',
    _5_minutes = '5 minutes',
    _10_minutes = '10 minutes',
    _15_minutes = '15 minutes',
    _30_minutes = '30 minutes',
    _1_hour = '1 hour',
    _6_hours = '6 hours',
    _12_hours = '12 hours',
    _1_day = '1 day',
    _1_week = '1 week',
    _1_month = '1 month'
}
/**
  * @export
  * @enum {string}
  */
export enum GetEntityHistorySortByEnum {
    created_at = 'created_at',
    attribute_id = 'attribute_id',
    value = 'value'
}
/**
  * @export
  * @enum {string}
  */
export enum GetEntityHistorySortDirectionEnum {
    asc = 'asc',
    desc = 'desc'
}
/**
  * @export
  * @enum {string}
  */
export enum SearchEntitiesOperationSortDirectionEnum {
    asc = 'asc',
    desc = 'desc'
}
