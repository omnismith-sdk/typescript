/* tslint:disable */
/* eslint-disable */
/**
 * Omnismith API
 * API Documentation for Omnismith
 *
 * The version of the OpenAPI document: 1.0.14
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateEntityActionRequest,
  CreateTemplateAction201Response,
  EntityActionResponse,
  ErrorResponse,
  ListTemplateActions200Response,
  ReorderEntityActionsRequest,
  ToggleEntityActionRequest,
  UpdateEntityActionRequest,
  ValidationErrorResponse,
} from '../models/index';
import {
    CreateEntityActionRequestFromJSON,
    CreateEntityActionRequestToJSON,
    CreateTemplateAction201ResponseFromJSON,
    CreateTemplateAction201ResponseToJSON,
    EntityActionResponseFromJSON,
    EntityActionResponseToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ListTemplateActions200ResponseFromJSON,
    ListTemplateActions200ResponseToJSON,
    ReorderEntityActionsRequestFromJSON,
    ReorderEntityActionsRequestToJSON,
    ToggleEntityActionRequestFromJSON,
    ToggleEntityActionRequestToJSON,
    UpdateEntityActionRequestFromJSON,
    UpdateEntityActionRequestToJSON,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/index';

export interface CreateTemplateActionRequest {
    templateId: string;
    createEntityActionRequest: CreateEntityActionRequest;
    xOmnismithProjectId?: string;
}

export interface DeleteTemplateActionRequest {
    templateId: string;
    id: string;
    xOmnismithProjectId?: string;
}

export interface GetTemplateActionRequest {
    templateId: string;
    id: string;
    xOmnismithProjectId?: string;
}

export interface ListTemplateActionsRequest {
    templateId: string;
    xOmnismithProjectId?: string;
    isEnabled?: boolean;
}

export interface ReorderTemplateActionsRequest {
    templateId: string;
    reorderEntityActionsRequest: ReorderEntityActionsRequest;
    xOmnismithProjectId?: string;
}

export interface ToggleTemplateActionRequest {
    templateId: string;
    id: string;
    toggleEntityActionRequest: ToggleEntityActionRequest;
    xOmnismithProjectId?: string;
}

export interface UpdateTemplateActionRequest {
    templateId: string;
    id: string;
    updateEntityActionRequest: UpdateEntityActionRequest;
    xOmnismithProjectId?: string;
}

/**
 * EntityActionApi - interface
 * 
 * @export
 * @interface EntityActionApiInterface
 */
export interface EntityActionApiInterface {
    /**
     * Creates request options for createTemplateAction without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {CreateEntityActionRequest} createEntityActionRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    createTemplateActionRequestOpts(requestParameters: CreateTemplateActionRequest): Promise<runtime.RequestOpts>;

    /**
     * Defines a named operation on records of a template. `precondition` gates availability (all conditions must hold against the record\'s current values; empty means always available), `fields` are the values the operator is asked for in dialog order, `presets` are written silently when the action runs. A status transition is `precondition: [status eq <draft-id>]`, `presets: [status = <confirmed-id>]`, no fields. The slug must be unique within the template. Fields and presets must name distinct non-metric attributes of the template and may not overlap. New actions are enabled and appended last.
     * @summary Create an action on a template
     * @param {string} templateId UUID or slug of the template
     * @param {CreateEntityActionRequest} createEntityActionRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    createTemplateActionRaw(requestParameters: CreateTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreateTemplateAction201Response>>;

    /**
     * Defines a named operation on records of a template. `precondition` gates availability (all conditions must hold against the record\'s current values; empty means always available), `fields` are the values the operator is asked for in dialog order, `presets` are written silently when the action runs. A status transition is `precondition: [status eq <draft-id>]`, `presets: [status = <confirmed-id>]`, no fields. The slug must be unique within the template. Fields and presets must name distinct non-metric attributes of the template and may not overlap. New actions are enabled and appended last.
     * Create an action on a template
     */
    createTemplateAction(requestParameters: CreateTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreateTemplateAction201Response>;

    /**
     * Creates request options for deleteTemplateAction without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Action UUID
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    deleteTemplateActionRequestOpts(requestParameters: DeleteTemplateActionRequest): Promise<runtime.RequestOpts>;

    /**
     * Permanently removes an action from its template. Records the action was run on are not affected.
     * @summary Delete an action
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Action UUID
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    deleteTemplateActionRaw(requestParameters: DeleteTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Permanently removes an action from its template. Records the action was run on are not affected.
     * Delete an action
     */
    deleteTemplateAction(requestParameters: DeleteTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * Creates request options for getTemplateAction without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Action UUID
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    getTemplateActionRequestOpts(requestParameters: GetTemplateActionRequest): Promise<runtime.RequestOpts>;

    /**
     * Returns one action of a template with its precondition, fields and presets.
     * @summary Get an action definition
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Action UUID
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    getTemplateActionRaw(requestParameters: GetTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityActionResponse>>;

    /**
     * Returns one action of a template with its precondition, fields and presets.
     * Get an action definition
     */
    getTemplateAction(requestParameters: GetTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityActionResponse>;

    /**
     * Creates request options for listTemplateActions without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {boolean} [isEnabled] Only enabled (&#x60;true&#x60;) or only disabled (&#x60;false&#x60;) actions
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    listTemplateActionsRequestOpts(requestParameters: ListTemplateActionsRequest): Promise<runtime.RequestOpts>;

    /**
     * Returns every action definition of a template in display order, including disabled ones unless filtered. An action is a named operation on one record: a precondition on current values, the fields the operator supplies, and presets written silently. To learn which actions apply to a specific record, and to run one, use the entity action endpoints.
     * @summary List the actions defined on a template
     * @param {string} templateId UUID or slug of the template
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {boolean} [isEnabled] Only enabled (&#x60;true&#x60;) or only disabled (&#x60;false&#x60;) actions
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    listTemplateActionsRaw(requestParameters: ListTemplateActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListTemplateActions200Response>>;

    /**
     * Returns every action definition of a template in display order, including disabled ones unless filtered. An action is a named operation on one record: a precondition on current values, the fields the operator supplies, and presets written silently. To learn which actions apply to a specific record, and to run one, use the entity action endpoints.
     * List the actions defined on a template
     */
    listTemplateActions(requestParameters: ListTemplateActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListTemplateActions200Response>;

    /**
     * Creates request options for reorderTemplateActions without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {ReorderEntityActionsRequest} reorderEntityActionsRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    reorderTemplateActionsRequestOpts(requestParameters: ReorderTemplateActionsRequest): Promise<runtime.RequestOpts>;

    /**
     * Sets the display order of a template\'s actions from an ordered list of every action UUID. The list must contain each action of the template exactly once; a partial or repeated list is rejected with 422. Returns the actions in their new order.
     * @summary Reorder the actions of a template
     * @param {string} templateId UUID or slug of the template
     * @param {ReorderEntityActionsRequest} reorderEntityActionsRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    reorderTemplateActionsRaw(requestParameters: ReorderTemplateActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListTemplateActions200Response>>;

    /**
     * Sets the display order of a template\'s actions from an ordered list of every action UUID. The list must contain each action of the template exactly once; a partial or repeated list is rejected with 422. Returns the actions in their new order.
     * Reorder the actions of a template
     */
    reorderTemplateActions(requestParameters: ReorderTemplateActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListTemplateActions200Response>;

    /**
     * Creates request options for toggleTemplateAction without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Action UUID
     * @param {ToggleEntityActionRequest} toggleEntityActionRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    toggleTemplateActionRequestOpts(requestParameters: ToggleTemplateActionRequest): Promise<runtime.RequestOpts>;

    /**
     * Turns an action on or off without changing its definition. Disabled actions are stored but neither offered on records nor executable.
     * @summary Enable or disable an action
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Action UUID
     * @param {ToggleEntityActionRequest} toggleEntityActionRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    toggleTemplateActionRaw(requestParameters: ToggleTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityActionResponse>>;

    /**
     * Turns an action on or off without changing its definition. Disabled actions are stored but neither offered on records nor executable.
     * Enable or disable an action
     */
    toggleTemplateAction(requestParameters: ToggleTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityActionResponse>;

    /**
     * Creates request options for updateTemplateAction without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Action UUID
     * @param {UpdateEntityActionRequest} updateEntityActionRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    updateTemplateActionRequestOpts(requestParameters: UpdateTemplateActionRequest): Promise<runtime.RequestOpts>;

    /**
     * Replaces the slug, name, description, icon, precondition, fields and presets of an action. The enabled flag and position are unchanged; use the toggle and order endpoints for those. The same validation as on create applies.
     * @summary Replace an action definition
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Action UUID
     * @param {UpdateEntityActionRequest} updateEntityActionRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityActionApiInterface
     */
    updateTemplateActionRaw(requestParameters: UpdateTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityActionResponse>>;

    /**
     * Replaces the slug, name, description, icon, precondition, fields and presets of an action. The enabled flag and position are unchanged; use the toggle and order endpoints for those. The same validation as on create applies.
     * Replace an action definition
     */
    updateTemplateAction(requestParameters: UpdateTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityActionResponse>;

}

/**
 * 
 */
export class EntityActionApi extends runtime.BaseAPI implements EntityActionApiInterface {

    /**
     * Creates request options for createTemplateAction without sending the request
     */
    async createTemplateActionRequestOpts(requestParameters: CreateTemplateActionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling createTemplateAction().'
            );
        }

        if (requestParameters['createEntityActionRequest'] == null) {
            throw new runtime.RequiredError(
                'createEntityActionRequest',
                'Required parameter "createEntityActionRequest" was null or undefined when calling createTemplateAction().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/actions`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateEntityActionRequestToJSON(requestParameters['createEntityActionRequest']),
        };
    }

    /**
     * Defines a named operation on records of a template. `precondition` gates availability (all conditions must hold against the record\'s current values; empty means always available), `fields` are the values the operator is asked for in dialog order, `presets` are written silently when the action runs. A status transition is `precondition: [status eq <draft-id>]`, `presets: [status = <confirmed-id>]`, no fields. The slug must be unique within the template. Fields and presets must name distinct non-metric attributes of the template and may not overlap. New actions are enabled and appended last.
     * Create an action on a template
     */
    async createTemplateActionRaw(requestParameters: CreateTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreateTemplateAction201Response>> {
        const requestOptions = await this.createTemplateActionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateTemplateAction201ResponseFromJSON(jsonValue));
    }

    /**
     * Defines a named operation on records of a template. `precondition` gates availability (all conditions must hold against the record\'s current values; empty means always available), `fields` are the values the operator is asked for in dialog order, `presets` are written silently when the action runs. A status transition is `precondition: [status eq <draft-id>]`, `presets: [status = <confirmed-id>]`, no fields. The slug must be unique within the template. Fields and presets must name distinct non-metric attributes of the template and may not overlap. New actions are enabled and appended last.
     * Create an action on a template
     */
    async createTemplateAction(requestParameters: CreateTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreateTemplateAction201Response> {
        const response = await this.createTemplateActionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteTemplateAction without sending the request
     */
    async deleteTemplateActionRequestOpts(requestParameters: DeleteTemplateActionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling deleteTemplateAction().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteTemplateAction().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/actions/{id}`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Permanently removes an action from its template. Records the action was run on are not affected.
     * Delete an action
     */
    async deleteTemplateActionRaw(requestParameters: DeleteTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deleteTemplateActionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Permanently removes an action from its template. Records the action was run on are not affected.
     * Delete an action
     */
    async deleteTemplateAction(requestParameters: DeleteTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTemplateActionRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for getTemplateAction without sending the request
     */
    async getTemplateActionRequestOpts(requestParameters: GetTemplateActionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling getTemplateAction().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getTemplateAction().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/actions/{id}`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns one action of a template with its precondition, fields and presets.
     * Get an action definition
     */
    async getTemplateActionRaw(requestParameters: GetTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityActionResponse>> {
        const requestOptions = await this.getTemplateActionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntityActionResponseFromJSON(jsonValue));
    }

    /**
     * Returns one action of a template with its precondition, fields and presets.
     * Get an action definition
     */
    async getTemplateAction(requestParameters: GetTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityActionResponse> {
        const response = await this.getTemplateActionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listTemplateActions without sending the request
     */
    async listTemplateActionsRequestOpts(requestParameters: ListTemplateActionsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling listTemplateActions().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['isEnabled'] != null) {
            queryParameters['is_enabled'] = requestParameters['isEnabled'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/actions`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns every action definition of a template in display order, including disabled ones unless filtered. An action is a named operation on one record: a precondition on current values, the fields the operator supplies, and presets written silently. To learn which actions apply to a specific record, and to run one, use the entity action endpoints.
     * List the actions defined on a template
     */
    async listTemplateActionsRaw(requestParameters: ListTemplateActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListTemplateActions200Response>> {
        const requestOptions = await this.listTemplateActionsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListTemplateActions200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns every action definition of a template in display order, including disabled ones unless filtered. An action is a named operation on one record: a precondition on current values, the fields the operator supplies, and presets written silently. To learn which actions apply to a specific record, and to run one, use the entity action endpoints.
     * List the actions defined on a template
     */
    async listTemplateActions(requestParameters: ListTemplateActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListTemplateActions200Response> {
        const response = await this.listTemplateActionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for reorderTemplateActions without sending the request
     */
    async reorderTemplateActionsRequestOpts(requestParameters: ReorderTemplateActionsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling reorderTemplateActions().'
            );
        }

        if (requestParameters['reorderEntityActionsRequest'] == null) {
            throw new runtime.RequiredError(
                'reorderEntityActionsRequest',
                'Required parameter "reorderEntityActionsRequest" was null or undefined when calling reorderTemplateActions().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/actions/order`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: ReorderEntityActionsRequestToJSON(requestParameters['reorderEntityActionsRequest']),
        };
    }

    /**
     * Sets the display order of a template\'s actions from an ordered list of every action UUID. The list must contain each action of the template exactly once; a partial or repeated list is rejected with 422. Returns the actions in their new order.
     * Reorder the actions of a template
     */
    async reorderTemplateActionsRaw(requestParameters: ReorderTemplateActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListTemplateActions200Response>> {
        const requestOptions = await this.reorderTemplateActionsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListTemplateActions200ResponseFromJSON(jsonValue));
    }

    /**
     * Sets the display order of a template\'s actions from an ordered list of every action UUID. The list must contain each action of the template exactly once; a partial or repeated list is rejected with 422. Returns the actions in their new order.
     * Reorder the actions of a template
     */
    async reorderTemplateActions(requestParameters: ReorderTemplateActionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListTemplateActions200Response> {
        const response = await this.reorderTemplateActionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for toggleTemplateAction without sending the request
     */
    async toggleTemplateActionRequestOpts(requestParameters: ToggleTemplateActionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling toggleTemplateAction().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling toggleTemplateAction().'
            );
        }

        if (requestParameters['toggleEntityActionRequest'] == null) {
            throw new runtime.RequiredError(
                'toggleEntityActionRequest',
                'Required parameter "toggleEntityActionRequest" was null or undefined when calling toggleTemplateAction().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/actions/{id}/toggle`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ToggleEntityActionRequestToJSON(requestParameters['toggleEntityActionRequest']),
        };
    }

    /**
     * Turns an action on or off without changing its definition. Disabled actions are stored but neither offered on records nor executable.
     * Enable or disable an action
     */
    async toggleTemplateActionRaw(requestParameters: ToggleTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityActionResponse>> {
        const requestOptions = await this.toggleTemplateActionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntityActionResponseFromJSON(jsonValue));
    }

    /**
     * Turns an action on or off without changing its definition. Disabled actions are stored but neither offered on records nor executable.
     * Enable or disable an action
     */
    async toggleTemplateAction(requestParameters: ToggleTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityActionResponse> {
        const response = await this.toggleTemplateActionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateTemplateAction without sending the request
     */
    async updateTemplateActionRequestOpts(requestParameters: UpdateTemplateActionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling updateTemplateAction().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateTemplateAction().'
            );
        }

        if (requestParameters['updateEntityActionRequest'] == null) {
            throw new runtime.RequiredError(
                'updateEntityActionRequest',
                'Required parameter "updateEntityActionRequest" was null or undefined when calling updateTemplateAction().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/actions/{id}`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateEntityActionRequestToJSON(requestParameters['updateEntityActionRequest']),
        };
    }

    /**
     * Replaces the slug, name, description, icon, precondition, fields and presets of an action. The enabled flag and position are unchanged; use the toggle and order endpoints for those. The same validation as on create applies.
     * Replace an action definition
     */
    async updateTemplateActionRaw(requestParameters: UpdateTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityActionResponse>> {
        const requestOptions = await this.updateTemplateActionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntityActionResponseFromJSON(jsonValue));
    }

    /**
     * Replaces the slug, name, description, icon, precondition, fields and presets of an action. The enabled flag and position are unchanged; use the toggle and order endpoints for those. The same validation as on create applies.
     * Replace an action definition
     */
    async updateTemplateAction(requestParameters: UpdateTemplateActionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityActionResponse> {
        const response = await this.updateTemplateActionRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
