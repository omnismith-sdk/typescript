/* tslint:disable */
/* eslint-disable */
/**
 * Omnismith API
 * API Documentation for Omnismith
 *
 * The version of the OpenAPI document: 1.0.14
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CreateEntityRuleRequest,
  CreateTemplateRule201Response,
  EntityRuleResponse,
  ErrorResponse,
  ListTemplateRules200Response,
  ToggleEntityRuleRequest,
  UpdateEntityRuleRequest,
  ValidationErrorResponse,
} from '../models/index';
import {
    CreateEntityRuleRequestFromJSON,
    CreateEntityRuleRequestToJSON,
    CreateTemplateRule201ResponseFromJSON,
    CreateTemplateRule201ResponseToJSON,
    EntityRuleResponseFromJSON,
    EntityRuleResponseToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ListTemplateRules200ResponseFromJSON,
    ListTemplateRules200ResponseToJSON,
    ToggleEntityRuleRequestFromJSON,
    ToggleEntityRuleRequestToJSON,
    UpdateEntityRuleRequestFromJSON,
    UpdateEntityRuleRequestToJSON,
    ValidationErrorResponseFromJSON,
    ValidationErrorResponseToJSON,
} from '../models/index';

export interface CreateTemplateRuleRequest {
    templateId: string;
    createEntityRuleRequest: CreateEntityRuleRequest;
    xOmnismithProjectId?: string;
}

export interface DeleteTemplateRuleRequest {
    templateId: string;
    id: string;
    xOmnismithProjectId?: string;
}

export interface GetTemplateRuleRequest {
    templateId: string;
    id: string;
    xOmnismithProjectId?: string;
}

export interface ListTemplateRulesRequest {
    templateId: string;
    xOmnismithProjectId?: string;
    isEnabled?: boolean;
}

export interface ToggleTemplateRuleRequest {
    templateId: string;
    id: string;
    toggleEntityRuleRequest: ToggleEntityRuleRequest;
    xOmnismithProjectId?: string;
}

export interface UpdateTemplateRuleRequest {
    templateId: string;
    id: string;
    updateEntityRuleRequest: UpdateEntityRuleRequest;
    xOmnismithProjectId?: string;
}

/**
 * EntityRuleApi - interface
 * 
 * @export
 * @interface EntityRuleApiInterface
 */
export interface EntityRuleApiInterface {
    /**
     * Creates request options for createTemplateRule without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {CreateEntityRuleRequest} createEntityRuleRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    createTemplateRuleRequestOpts(requestParameters: CreateTemplateRuleRequest): Promise<runtime.RequestOpts>;

    /**
     * Adds a rule of the form `when [conditions] then [constraints]` to a template. Rules are enforced synchronously in every entity write path (create, update, replace, batch, CSV import, MCP): when every `when` condition holds against the record\'s effective state, every `then` constraint must hold, otherwise the write is rejected with 422 and `errors` keyed by `attributes.<slug>`. An empty `when` makes the rule unconditional, which is how a required field is expressed (`then: [{attribute, is_not_empty}]`). Referenced attributes must belong to the template and must not be metrics; operators must fit the attribute\'s data type.
     * @summary Create a business rule on a template
     * @param {string} templateId UUID or slug of the template
     * @param {CreateEntityRuleRequest} createEntityRuleRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    createTemplateRuleRaw(requestParameters: CreateTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreateTemplateRule201Response>>;

    /**
     * Adds a rule of the form `when [conditions] then [constraints]` to a template. Rules are enforced synchronously in every entity write path (create, update, replace, batch, CSV import, MCP): when every `when` condition holds against the record\'s effective state, every `then` constraint must hold, otherwise the write is rejected with 422 and `errors` keyed by `attributes.<slug>`. An empty `when` makes the rule unconditional, which is how a required field is expressed (`then: [{attribute, is_not_empty}]`). Referenced attributes must belong to the template and must not be metrics; operators must fit the attribute\'s data type.
     * Create a business rule on a template
     */
    createTemplateRule(requestParameters: CreateTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreateTemplateRule201Response>;

    /**
     * Creates request options for deleteTemplateRule without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Rule UUID
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    deleteTemplateRuleRequestOpts(requestParameters: DeleteTemplateRuleRequest): Promise<runtime.RequestOpts>;

    /**
     * Permanently removes a rule from its template. Existing records are not re-validated.
     * @summary Delete a business rule
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Rule UUID
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    deleteTemplateRuleRaw(requestParameters: DeleteTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Permanently removes a rule from its template. Existing records are not re-validated.
     * Delete a business rule
     */
    deleteTemplateRule(requestParameters: DeleteTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * Creates request options for getTemplateRule without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Rule UUID
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    getTemplateRuleRequestOpts(requestParameters: GetTemplateRuleRequest): Promise<runtime.RequestOpts>;

    /**
     * Returns one rule of a template with its conditions and constraints.
     * @summary Get a business rule
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Rule UUID
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    getTemplateRuleRaw(requestParameters: GetTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityRuleResponse>>;

    /**
     * Returns one rule of a template with its conditions and constraints.
     * Get a business rule
     */
    getTemplateRule(requestParameters: GetTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityRuleResponse>;

    /**
     * Creates request options for listTemplateRules without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {boolean} [isEnabled] Only enabled (&#x60;true&#x60;) or only disabled (&#x60;false&#x60;) rules
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    listTemplateRulesRequestOpts(requestParameters: ListTemplateRulesRequest): Promise<runtime.RequestOpts>;

    /**
     * Returns every rule defined on a template in creation order, including disabled ones unless filtered. Read these before writing entities of the template: they are the invariants a create, update, replace, batch or import must satisfy, and a violation returns 422 with `errors` keyed by `attributes.<slug>`.
     * @summary List the business rules of a template
     * @param {string} templateId UUID or slug of the template
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {boolean} [isEnabled] Only enabled (&#x60;true&#x60;) or only disabled (&#x60;false&#x60;) rules
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    listTemplateRulesRaw(requestParameters: ListTemplateRulesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListTemplateRules200Response>>;

    /**
     * Returns every rule defined on a template in creation order, including disabled ones unless filtered. Read these before writing entities of the template: they are the invariants a create, update, replace, batch or import must satisfy, and a violation returns 422 with `errors` keyed by `attributes.<slug>`.
     * List the business rules of a template
     */
    listTemplateRules(requestParameters: ListTemplateRulesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListTemplateRules200Response>;

    /**
     * Creates request options for toggleTemplateRule without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Rule UUID
     * @param {ToggleEntityRuleRequest} toggleEntityRuleRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    toggleTemplateRuleRequestOpts(requestParameters: ToggleTemplateRuleRequest): Promise<runtime.RequestOpts>;

    /**
     * Turns enforcement of a rule on or off without changing its definition. Disabled rules are stored but skipped by every write path.
     * @summary Enable or disable a business rule
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Rule UUID
     * @param {ToggleEntityRuleRequest} toggleEntityRuleRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    toggleTemplateRuleRaw(requestParameters: ToggleTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityRuleResponse>>;

    /**
     * Turns enforcement of a rule on or off without changing its definition. Disabled rules are stored but skipped by every write path.
     * Enable or disable a business rule
     */
    toggleTemplateRule(requestParameters: ToggleTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityRuleResponse>;

    /**
     * Creates request options for updateTemplateRule without sending the request
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Rule UUID
     * @param {UpdateEntityRuleRequest} updateEntityRuleRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    updateTemplateRuleRequestOpts(requestParameters: UpdateTemplateRuleRequest): Promise<runtime.RequestOpts>;

    /**
     * Replaces the name, message, conditions and constraints of a rule. The enabled flag is unchanged; use the toggle endpoint for that. The same validation as on create applies: attributes must belong to the template and must not be metrics, operators must fit the attribute\'s data type.
     * @summary Replace a business rule
     * @param {string} templateId UUID or slug of the template
     * @param {string} id Rule UUID
     * @param {UpdateEntityRuleRequest} updateEntityRuleRequest 
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EntityRuleApiInterface
     */
    updateTemplateRuleRaw(requestParameters: UpdateTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityRuleResponse>>;

    /**
     * Replaces the name, message, conditions and constraints of a rule. The enabled flag is unchanged; use the toggle endpoint for that. The same validation as on create applies: attributes must belong to the template and must not be metrics, operators must fit the attribute\'s data type.
     * Replace a business rule
     */
    updateTemplateRule(requestParameters: UpdateTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityRuleResponse>;

}

/**
 * 
 */
export class EntityRuleApi extends runtime.BaseAPI implements EntityRuleApiInterface {

    /**
     * Creates request options for createTemplateRule without sending the request
     */
    async createTemplateRuleRequestOpts(requestParameters: CreateTemplateRuleRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling createTemplateRule().'
            );
        }

        if (requestParameters['createEntityRuleRequest'] == null) {
            throw new runtime.RequiredError(
                'createEntityRuleRequest',
                'Required parameter "createEntityRuleRequest" was null or undefined when calling createTemplateRule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/rules`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateEntityRuleRequestToJSON(requestParameters['createEntityRuleRequest']),
        };
    }

    /**
     * Adds a rule of the form `when [conditions] then [constraints]` to a template. Rules are enforced synchronously in every entity write path (create, update, replace, batch, CSV import, MCP): when every `when` condition holds against the record\'s effective state, every `then` constraint must hold, otherwise the write is rejected with 422 and `errors` keyed by `attributes.<slug>`. An empty `when` makes the rule unconditional, which is how a required field is expressed (`then: [{attribute, is_not_empty}]`). Referenced attributes must belong to the template and must not be metrics; operators must fit the attribute\'s data type.
     * Create a business rule on a template
     */
    async createTemplateRuleRaw(requestParameters: CreateTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreateTemplateRule201Response>> {
        const requestOptions = await this.createTemplateRuleRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateTemplateRule201ResponseFromJSON(jsonValue));
    }

    /**
     * Adds a rule of the form `when [conditions] then [constraints]` to a template. Rules are enforced synchronously in every entity write path (create, update, replace, batch, CSV import, MCP): when every `when` condition holds against the record\'s effective state, every `then` constraint must hold, otherwise the write is rejected with 422 and `errors` keyed by `attributes.<slug>`. An empty `when` makes the rule unconditional, which is how a required field is expressed (`then: [{attribute, is_not_empty}]`). Referenced attributes must belong to the template and must not be metrics; operators must fit the attribute\'s data type.
     * Create a business rule on a template
     */
    async createTemplateRule(requestParameters: CreateTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreateTemplateRule201Response> {
        const response = await this.createTemplateRuleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteTemplateRule without sending the request
     */
    async deleteTemplateRuleRequestOpts(requestParameters: DeleteTemplateRuleRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling deleteTemplateRule().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteTemplateRule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/rules/{id}`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Permanently removes a rule from its template. Existing records are not re-validated.
     * Delete a business rule
     */
    async deleteTemplateRuleRaw(requestParameters: DeleteTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deleteTemplateRuleRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Permanently removes a rule from its template. Existing records are not re-validated.
     * Delete a business rule
     */
    async deleteTemplateRule(requestParameters: DeleteTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTemplateRuleRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for getTemplateRule without sending the request
     */
    async getTemplateRuleRequestOpts(requestParameters: GetTemplateRuleRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling getTemplateRule().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getTemplateRule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/rules/{id}`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns one rule of a template with its conditions and constraints.
     * Get a business rule
     */
    async getTemplateRuleRaw(requestParameters: GetTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityRuleResponse>> {
        const requestOptions = await this.getTemplateRuleRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntityRuleResponseFromJSON(jsonValue));
    }

    /**
     * Returns one rule of a template with its conditions and constraints.
     * Get a business rule
     */
    async getTemplateRule(requestParameters: GetTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityRuleResponse> {
        const response = await this.getTemplateRuleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listTemplateRules without sending the request
     */
    async listTemplateRulesRequestOpts(requestParameters: ListTemplateRulesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling listTemplateRules().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['isEnabled'] != null) {
            queryParameters['is_enabled'] = requestParameters['isEnabled'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/rules`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns every rule defined on a template in creation order, including disabled ones unless filtered. Read these before writing entities of the template: they are the invariants a create, update, replace, batch or import must satisfy, and a violation returns 422 with `errors` keyed by `attributes.<slug>`.
     * List the business rules of a template
     */
    async listTemplateRulesRaw(requestParameters: ListTemplateRulesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListTemplateRules200Response>> {
        const requestOptions = await this.listTemplateRulesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListTemplateRules200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns every rule defined on a template in creation order, including disabled ones unless filtered. Read these before writing entities of the template: they are the invariants a create, update, replace, batch or import must satisfy, and a violation returns 422 with `errors` keyed by `attributes.<slug>`.
     * List the business rules of a template
     */
    async listTemplateRules(requestParameters: ListTemplateRulesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListTemplateRules200Response> {
        const response = await this.listTemplateRulesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for toggleTemplateRule without sending the request
     */
    async toggleTemplateRuleRequestOpts(requestParameters: ToggleTemplateRuleRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling toggleTemplateRule().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling toggleTemplateRule().'
            );
        }

        if (requestParameters['toggleEntityRuleRequest'] == null) {
            throw new runtime.RequiredError(
                'toggleEntityRuleRequest',
                'Required parameter "toggleEntityRuleRequest" was null or undefined when calling toggleTemplateRule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/rules/{id}/toggle`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ToggleEntityRuleRequestToJSON(requestParameters['toggleEntityRuleRequest']),
        };
    }

    /**
     * Turns enforcement of a rule on or off without changing its definition. Disabled rules are stored but skipped by every write path.
     * Enable or disable a business rule
     */
    async toggleTemplateRuleRaw(requestParameters: ToggleTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityRuleResponse>> {
        const requestOptions = await this.toggleTemplateRuleRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntityRuleResponseFromJSON(jsonValue));
    }

    /**
     * Turns enforcement of a rule on or off without changing its definition. Disabled rules are stored but skipped by every write path.
     * Enable or disable a business rule
     */
    async toggleTemplateRule(requestParameters: ToggleTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityRuleResponse> {
        const response = await this.toggleTemplateRuleRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateTemplateRule without sending the request
     */
    async updateTemplateRuleRequestOpts(requestParameters: UpdateTemplateRuleRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['templateId'] == null) {
            throw new runtime.RequiredError(
                'templateId',
                'Required parameter "templateId" was null or undefined when calling updateTemplateRule().'
            );
        }

        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateTemplateRule().'
            );
        }

        if (requestParameters['updateEntityRuleRequest'] == null) {
            throw new runtime.RequiredError(
                'updateEntityRuleRequest',
                'Required parameter "updateEntityRuleRequest" was null or undefined when calling updateTemplateRule().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/templates/{templateId}/rules/{id}`;
        urlPath = urlPath.replace(`{${"templateId"}}`, encodeURIComponent(String(requestParameters['templateId'])));
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateEntityRuleRequestToJSON(requestParameters['updateEntityRuleRequest']),
        };
    }

    /**
     * Replaces the name, message, conditions and constraints of a rule. The enabled flag is unchanged; use the toggle endpoint for that. The same validation as on create applies: attributes must belong to the template and must not be metrics, operators must fit the attribute\'s data type.
     * Replace a business rule
     */
    async updateTemplateRuleRaw(requestParameters: UpdateTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<EntityRuleResponse>> {
        const requestOptions = await this.updateTemplateRuleRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => EntityRuleResponseFromJSON(jsonValue));
    }

    /**
     * Replaces the name, message, conditions and constraints of a rule. The enabled flag is unchanged; use the toggle endpoint for that. The same validation as on create applies: attributes must belong to the template and must not be metrics, operators must fit the attribute\'s data type.
     * Replace a business rule
     */
    async updateTemplateRule(requestParameters: UpdateTemplateRuleRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<EntityRuleResponse> {
        const response = await this.updateTemplateRuleRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
