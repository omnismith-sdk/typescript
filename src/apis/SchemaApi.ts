/* tslint:disable */
/* eslint-disable */
/**
 * Omnismith API
 * API Documentation for Omnismith
 *
 * The version of the OpenAPI document: 1.0.14
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ErrorResponse,
  ProjectSchemaResponse,
  UiLayoutResponse,
} from '../models/index';
import {
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ProjectSchemaResponseFromJSON,
    ProjectSchemaResponseToJSON,
    UiLayoutResponseFromJSON,
    UiLayoutResponseToJSON,
} from '../models/index';

export interface GetProjectSchemaRequest {
    xOmnismithProjectId?: string;
}

export interface GetUiLayoutRequest {
    xOmnismithProjectId?: string;
}

/**
 * SchemaApi - interface
 * 
 * @export
 * @interface SchemaApiInterface
 */
export interface SchemaApiInterface {
    /**
     * Creates request options for getProjectSchema without sending the request
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof SchemaApiInterface
     */
    getProjectSchemaRequestOpts(requestParameters: GetProjectSchemaRequest): Promise<runtime.RequestOpts>;

    /**
     * Retrieves the consolidated schema graph for the active project in a concise, token-efficient shape. Includes all active templates (with bound attributes, business rules, and executable actions) and attributes (with semantic types, list choice options, and foreign entity reference targets). Ideal for AI agents, client initialization, metadata caching, and schema introspection.
     * @summary Get complete project schema graph
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SchemaApiInterface
     */
    getProjectSchemaRaw(requestParameters: GetProjectSchemaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectSchemaResponse>>;

    /**
     * Retrieves the consolidated schema graph for the active project in a concise, token-efficient shape. Includes all active templates (with bound attributes, business rules, and executable actions) and attributes (with semantic types, list choice options, and foreign entity reference targets). Ideal for AI agents, client initialization, metadata caching, and schema introspection.
     * Get complete project schema graph
     */
    getProjectSchema(requestParameters: GetProjectSchemaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectSchemaResponse>;

    /**
     * Creates request options for getUiLayout without sending the request
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @throws {RequiredError}
     * @memberof SchemaApiInterface
     */
    getUiLayoutRequestOpts(requestParameters: GetUiLayoutRequest): Promise<runtime.RequestOpts>;

    /**
     * Retrieves visual form layout configurations for the active project including template section groups, grid column counts, header icons, and action display sort orders.
     * @summary Get project UI layout configurations
     * @param {string} [xOmnismithProjectId] The project this call acts on. A credential proves identity and grants a set of projects; it never selects one, so every tenant-scoped call names its target here. The value must be one of the projects in the credential\&#39;s &#x60;projects&#x60; claim and must still be reachable — a project the caller is not a member of, or one that has been deleted, is rejected with 403 rather than silently ignored. A project the caller *is* a member of but which the credential predates is also rejected with 403, carrying the code &#x60;stale_project_grant&#x60;; that one is answered by refreshing the credential once and retrying, and is the only 403 here worth retrying. Omitting the header is not an error: the caller is simply acting with no project selected, and a tenant-scoped operation then answers 409 &#x60;no_project_selected&#x60;. Two clients holding the same credential may send different values at the same time.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SchemaApiInterface
     */
    getUiLayoutRaw(requestParameters: GetUiLayoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UiLayoutResponse>>;

    /**
     * Retrieves visual form layout configurations for the active project including template section groups, grid column counts, header icons, and action display sort orders.
     * Get project UI layout configurations
     */
    getUiLayout(requestParameters: GetUiLayoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UiLayoutResponse>;

}

/**
 * 
 */
export class SchemaApi extends runtime.BaseAPI implements SchemaApiInterface {

    /**
     * Creates request options for getProjectSchema without sending the request
     */
    async getProjectSchemaRequestOpts(requestParameters: GetProjectSchemaRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/discovery/project-schema`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Retrieves the consolidated schema graph for the active project in a concise, token-efficient shape. Includes all active templates (with bound attributes, business rules, and executable actions) and attributes (with semantic types, list choice options, and foreign entity reference targets). Ideal for AI agents, client initialization, metadata caching, and schema introspection.
     * Get complete project schema graph
     */
    async getProjectSchemaRaw(requestParameters: GetProjectSchemaRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProjectSchemaResponse>> {
        const requestOptions = await this.getProjectSchemaRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProjectSchemaResponseFromJSON(jsonValue));
    }

    /**
     * Retrieves the consolidated schema graph for the active project in a concise, token-efficient shape. Includes all active templates (with bound attributes, business rules, and executable actions) and attributes (with semantic types, list choice options, and foreign entity reference targets). Ideal for AI agents, client initialization, metadata caching, and schema introspection.
     * Get complete project schema graph
     */
    async getProjectSchema(requestParameters: GetProjectSchemaRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProjectSchemaResponse> {
        const response = await this.getProjectSchemaRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getUiLayout without sending the request
     */
    async getUiLayoutRequestOpts(requestParameters: GetUiLayoutRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['xOmnismithProjectId'] != null) {
            headerParameters['X-Omnismith-Project-Id'] = String(requestParameters['xOmnismithProjectId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/discovery/ui-layout`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Retrieves visual form layout configurations for the active project including template section groups, grid column counts, header icons, and action display sort orders.
     * Get project UI layout configurations
     */
    async getUiLayoutRaw(requestParameters: GetUiLayoutRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<UiLayoutResponse>> {
        const requestOptions = await this.getUiLayoutRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => UiLayoutResponseFromJSON(jsonValue));
    }

    /**
     * Retrieves visual form layout configurations for the active project including template section groups, grid column counts, header icons, and action display sort orders.
     * Get project UI layout configurations
     */
    async getUiLayout(requestParameters: GetUiLayoutRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<UiLayoutResponse> {
        const response = await this.getUiLayoutRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
